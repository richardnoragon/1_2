[
  {
    "filename": "exif_editor_logic.py",
    "content": "import os\nimport piexif\nimport piexif.helper\nfrom PyQt5.QtCore import QObject, pyqtSignal\nfrom PIL import Image # Needed by piexif for some operations/checks\nfrom PIL.ExifTags import TAGS as PIL_TAGS # For mapping codes to names\n\n# Combine piexif's tags with PIL's for better name resolution\nALL_KNOWN_TAGS = {\n    ifd: {code: piexif.TAGS[ifd][code].get('name', f'UnknownTag_{code}') \n          for code in piexif.TAGS[ifd]} \n    for ifd in piexif.TAGS\n}\n# Add PIL tags if missing (might be duplicates, piexif takes precedence)\nfor code, name in PIL_TAGS.items():\n    found = False\n    for ifd_tags in ALL_KNOWN_TAGS.values():\n        if code in ifd_tags:\n            found = True\n            break\n    if not found:\n         # Add to a default IFD like '0th' if not found anywhere\n         if 0xFFFE < code: # Treat higher codes generally\n             if 'Exif' not in ALL_KNOWN_TAGS: ALL_KNOWN_TAGS['Exif'] = {}\n             if code not in ALL_KNOWN_TAGS['Exif']: ALL_KNOWN_TAGS['Exif'][code] = name\n         else:\n             if '0th' not in ALL_KNOWN_TAGS: ALL_KNOWN_TAGS['0th'] = {}\n             if code not in ALL_KNOWN_TAGS['0th']: ALL_KNOWN_TAGS['0th'][code] = name\n\ndef get_tag_name(ifd_name, tag_code):\n    \"\"\"Gets a human-readable tag name.\"\"\"\n    if ifd_name in ALL_KNOWN_TAGS and tag_code in ALL_KNOWN_TAGS[ifd_name]:\n        return ALL_KNOWN_TAGS[ifd_name][tag_code]\n    # Fallback using PIL tags directly by code\n    return PIL_TAGS.get(tag_code, f'UnknownTag_{tag_code}')\n\ndef format_exif_value(value):\n    \"\"\"Formats EXIF value for display, handling bytes.\"\"\"\n    if isinstance(value, bytes):\n        try:\n            # Try decoding common text encodings\n            return value.decode('utf-8').strip('\\x00 ')\n        except UnicodeDecodeError:\n            # Handle specific known byte structures or return hex/repr\n            if len(value) > 16: # Avoid huge hex strings\n                return f'{value[:16].hex()}... (bytes)'\n            else:\n                return f'{value.hex()} (bytes)' # Or repr(value)\n    elif isinstance(value, tuple) and all(isinstance(x, int) for x in value):\n         # Often represents rational (num, den), display as fraction or float\n         if len(value) == 2 and value[1] != 0:\n              # Heuristic: If den is 1 or num/den is simple float, show float\n              if value[1] == 1:\n                   return str(value[0])\n              # Show as fraction for typical EXIF rationals (exposure, aperture)\n              # Avoid float conversion for precision (e.g., 1/3) \n              return f\"{value[0]}/{value[1]}\"\n         else:\n             # Other tuples just show as string representation\n              return str(value)\n    return str(value)\n\ndef parse_exif_value(value_str, original_type, tag_code):\n    \"\"\"Attempts to parse string input back to appropriate EXIF type.\"\"\"\n    # Simple heuristic based on original type\n    if isinstance(original_type, bytes):\n        # Try encoding back to UTF-8, common for text tags stored as bytes\n        # Some tags (e.g., UserComment) have specific encoding prefixes\n        if tag_code == piexif.ExifIFD.UserComment:\n             # Needs encoding preamble (e.g., ASCII\\x00\\x00\\x00...)\n             # Piexif helper handles this if given a string\n             return value_str\n        try:\n             return value_str.encode('utf-8')\n        except Exception:\n             raise ValueError(f\"Cannot encode '{value_str}' back to bytes for this tag.\")\n    elif isinstance(original_type, int):\n        try:\n            return int(value_str)\n        except ValueError:\n            raise ValueError(f\"Invalid integer value: '{value_str}'\")\n    elif isinstance(original_type, tuple) and len(original_type) == 2 and all(isinstance(x, int) for x in original_type):\n        # Handle rational (fraction or float input)\n        try:\n            if '/' in value_str:\n                num, den = map(int, value_str.split('/', 1))\n                if den == 0: raise ValueError(\"Denominator cannot be zero\")\n                # TODO: Simplify fraction if needed?\n                return (num, den)\n            else:\n                # Try converting float to rational (might lose precision)\n                # Piexif might handle float input directly for some tags?\n                # For simplicity, require fraction input for now.\n                # Or attempt float -> Fraction -> num/den? Requires 'fractions' module.\n                f_val = float(value_str)\n                # Basic float to rational - limited precision\n                int_part = int(f_val)\n                frac_part = f_val - int_part\n                # Simple conversion might work for some cases\n                if frac_part == 0: return (int_part, 1)\n                # More complex conversion needed for general floats... Requires fractions module\n                # from fractions import Fraction\n                # frac = Fraction(f_val).limit_denominator()\n                # return (frac.numerator, frac.denominator)\n                raise ValueError(\"Use fraction format (e.g., 1/100) for rational values, float conversion not fully implemented.\")\n        except ValueError as e:\n            raise ValueError(f\"Invalid rational value format: '{value_str}'. Use 'num/den'. Error: {e}\")\n    else:\n        # Default to string (covers ASCII tags etc.)\n        return value_str\n\nclass ExifEditorLogic(QObject):\n    \"\"\"Handles loading and saving EXIF data using piexif.\"\"\"\n    exif_data_loaded = pyqtSignal(dict) # {ifd_name: {tag_code: {'name': name, 'value': value, 'original_type': type}}} \n    save_result = pyqtSignal(bool, str) # success, message\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self):\n        super().__init__()\n        self._filepath = None\n        self.original_exif_dict = None # Store the raw loaded dict\n\n    def load_exif(self, filepath):\n        \"\"\"Loads EXIF data from the specified image file.\"\"\"\n        self._filepath = filepath\n        self.original_exif_dict = None\n        processed_data = {}\n        try:\n            if not os.path.exists(filepath):\n                raise FileNotFoundError(f\"File not found: {filepath}\")\n            \n            # Check if it's likely an image piexif can handle (basic check)\n            try:\n                 # Pillow check can be more robust \n                 img = Image.open(filepath)\n                 img.verify() # Verify structure \n                 fmt = img.format\n                 if fmt not in ('JPEG', 'TIFF'):\n                      raise ValueError(f\"Unsupported format: {fmt}. Only JPEG/TIFF supported.\")\n                 # Check if file actually has EXIF before trying piexif load\n                 # Pillow's _getexif() is one way, piexif.load is another\n            except Exception as pil_e:\n                 # Could be not an image, or corrupted\n                 raise ValueError(f\"Cannot open or verify image file: {pil_e}\")\n\n            # Load EXIF data using piexif\n            self.original_exif_dict = piexif.load(filepath)\n            if not self.original_exif_dict or not any(self.original_exif_dict.values()):\n                # Check if dict is empty or all IFDs are empty\n                self.exif_data_loaded.emit({}) # Emit empty dict to indicate no EXIF\n                self.finished.emit()\n                return\n\n            # Process the dictionary for display\n            for ifd_name in self.original_exif_dict:\n                if ifd_name == 'thumbnail': continue # Skip thumbnail data itself\n                if not self.original_exif_dict[ifd_name]: continue # Skip empty IFDs\n                \n                processed_data[ifd_name] = {}\n                for tag_code, value in self.original_exif_dict[ifd_name].items():\n                    tag_name = get_tag_name(ifd_name, tag_code)\n                    display_value = format_exif_value(value)\n                    processed_data[ifd_name][tag_code] = {\n                        'name': tag_name,\n                        'value': display_value,\n                        'original_value': value, # Store original for type parsing later\n                        'original_type': type(value)\n                    }\n            \n            self.exif_data_loaded.emit(processed_data)\n\n        except FileNotFoundError as e:\n             self.error_occurred.emit(str(e))\n        except ValueError as e:\n             self.error_occurred.emit(str(e))\n        except piexif.InvalidImageDataError:\n             self.error_occurred.emit(f\"Invalid image data or unsupported format: {filepath}\")\n        except PermissionError:\n             self.error_occurred.emit(f\"Permission denied reading file: {filepath}\")\n        except Exception as e:\n            # Catch-all for other piexif/IO errors\n            self.error_occurred.emit(f\"Error loading EXIF data: {e}\")\n        finally:\n            self.finished.emit()\n\n    def save_exif(self, filepath, modified_data):\n        \"\"\"Saves the modified EXIF data back to the file.\"\"\"\n        try:\n            if not self.original_exif_dict:\n                # This shouldn't happen if load was successful, but check\n                # Maybe allow creating EXIF from scratch? More complex.\n                # For now, assume we're modifying existing.\n                raise RuntimeError(\"Original EXIF data not loaded. Cannot save.\")\n            if not os.path.exists(filepath):\n                raise FileNotFoundError(f\"File not found (was it moved/deleted?): {filepath}\")\n\n            # Create a new exif dictionary based on modifications\n            new_exif_dict = {ifd: {} for ifd in self.original_exif_dict if ifd != 'thumbnail'}\n            # Ensure all original IFDs exist in the new dict\n            for ifd_name in self.original_exif_dict:\n                 if ifd_name != 'thumbnail' and ifd_name not in new_exif_dict:\n                     new_exif_dict[ifd_name] = {}\n                     \n            if 'thumbnail' in self.original_exif_dict:\n                 new_exif_dict['thumbnail'] = self.original_exif_dict['thumbnail'] # Preserve original thumbnail\n\n            # Populate the new dictionary with parsed values from GUI data\n            for ifd_name, tags in modified_data.items():\n                if ifd_name not in new_exif_dict:\n                    self.error_occurred.emit(f\"Warning: IFD '{ifd_name}' not in original data, skipping.\")\n                    continue\n                for tag_code, tag_info in tags.items():\n                    original_type = tag_info['original_type']\n                    current_value_str = tag_info['value']\n                    original_value = tag_info['original_value']\n                    \n                    # Check if value was actually modified\n                    original_display_val = format_exif_value(original_value)\n                    if current_value_str == original_display_val:\n                         # Value unchanged, use original raw value\n                         new_exif_dict[ifd_name][tag_code] = original_value\n                    else:\n                        # Value changed, try to parse it back\n                        try:\n                            parsed_value = parse_exif_value(current_value_str, original_type, tag_code)\n                            # Handle specific helper cases (like UserComment)\n                            if tag_code == piexif.ExifIFD.UserComment and isinstance(parsed_value, str):\n                                 new_exif_dict[ifd_name][tag_code] = piexif.helper.UserComment.dump(parsed_value)\n                            else:\n                                 new_exif_dict[ifd_name][tag_code] = parsed_value\n                        except ValueError as e:\n                            raise ValueError(f\"Error parsing value for tag '{tag_info['name']}' ({ifd_name}/{tag_code}): {e}\")\n                            \n            # Remove empty IFDs before dumping\n            ifds_to_remove = [ifd for ifd, tags in new_exif_dict.items() if not tags and ifd != 'thumbnail']\n            for ifd in ifds_to_remove:\n                 del new_exif_dict[ifd]\n                 \n            # Dump the EXIF data into bytes\n            exif_bytes = piexif.dump(new_exif_dict)\n\n            # Insert the bytes back into the image file\n            piexif.insert(exif_bytes, filepath)\n\n            self.save_result.emit(True, f\"EXIF data successfully saved to {os.path.basename(filepath)}.\")\n\n        except FileNotFoundError as e:\n             self.error_occurred.emit(str(e))\n             self.save_result.emit(False, str(e))\n        except ValueError as e:\n             self.error_occurred.emit(str(e))\n             self.save_result.emit(False, str(e))\n        except RuntimeError as e:\n             self.error_occurred.emit(str(e))\n             self.save_result.emit(False, str(e))\n        except piexif.InvalidImageDataError:\n             err_msg = f\"Invalid image data or unsupported format for saving: {filepath}\"\n             self.error_occurred.emit(err_msg)\n             self.save_result.emit(False, err_msg)\n        except PermissionError:\n             err_msg = f\"Permission denied writing file: {filepath}\"\n             self.error_occurred.emit(err_msg)\n             self.save_result.emit(False, err_msg)\n        except Exception as e:\n            err_msg = f\"Error saving EXIF data: {e}\"\n            self.error_occurred.emit(err_msg)\n            self.save_result.emit(False, err_msg)\n        finally:\n            self.finished.emit()\n"
  },
  {
    "filename": "exif_editor_gui.py",
    "content": "import sys\nimport os\nfrom PyQt5.QtWidgets import (\n    QApplication, QMainWindow, QWidget, QVBoxLayout, QHBoxLayout,\n    QPushButton, QLabel, QLineEdit, QFileDialog, QStatusBar,\n    QMessageBox, QTreeWidget, QTreeWidgetItem, QAbstractItemView,\n    QHeaderView\n)\nfrom PyQt5.QtCore import Qt, QThread, pyqtSignal, QObject\n\nfrom exif_editor_logic import ExifEditorLogic\n\n# Define column indices for the tree widget\nCOL_IFD = 0\nCOL_TAG_NAME = 1\nCOL_VALUE = 2\n\nclass Worker(QObject):\n    \"\"\"Worker thread for EXIF operations.\"\"\"\n    exif_data_loaded = pyqtSignal(dict)\n    save_result = pyqtSignal(bool, str)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, logic_instance):\n        super().__init__()\n        self.logic = logic_instance\n        self._mode = None # 'load' or 'save'\n        self._args = []\n        self._kwargs = {}\n\n    def run(self):\n        # Connect logic signals\n        self.logic.exif_data_loaded.connect(self.exif_data_loaded)\n        self.logic.save_result.connect(self.save_result)\n        self.logic.error_occurred.connect(self.error_occurred)\n        self.logic.finished.connect(self.on_logic_finished)\n\n        try:\n            if self._mode == 'load':\n                self.logic.load_exif(*self._args, **self._kwargs)\n            elif self._mode == 'save':\n                self.logic.save_exif(*self._args, **self._kwargs)\n            else:\n                self.error_occurred.emit(\"Worker started with invalid mode.\")\n                self.on_logic_finished()\n        except Exception as e:\n            self.error_occurred.emit(f\"Critical error in worker execution: {e}\")\n            self.on_logic_finished()\n\n    def set_task(self, mode, args=None, kwargs=None):\n        self._mode = mode\n        self._args = args if args is not None else []\n        self._kwargs = kwargs if kwargs is not None else {}\n\n    def on_logic_finished(self):\n        # Disconnect signals\n        try:\n            self.logic.exif_data_loaded.disconnect(self.exif_data_loaded)\n            self.logic.save_result.disconnect(self.save_result)\n            self.logic.error_occurred.disconnect(self.error_occurred)\n            self.logic.finished.disconnect(self.on_logic_finished)\n        except TypeError:\n            pass # Ignore if already disconnected\n        self.finished.emit()\n\nclass ExifEditorApp(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle(\"EXIF Metadata Editor\")\n        self.setGeometry(100, 100, 750, 600)\n\n        self.central_widget = QWidget()\n        self.setCentralWidget(self.central_widget)\n        self.main_layout = QVBoxLayout(self.central_widget)\n\n        # Store the currently displayed/modified data\n        self.current_exif_data = {}\n\n        # --- Worker Thread Setup ---\n        self.thread = None\n        self.worker = None\n        self.logic = ExifEditorLogic()\n\n        # --- File Selection ---\n        file_layout = QHBoxLayout()\n        file_label = QLabel(\"Image File (JPEG/TIFF):\")\n        self.file_edit = QLineEdit()\n        self.file_edit.setPlaceholderText(\"Select an image file to view/edit EXIF data...\")\n        self.file_edit.setReadOnly(True)\n        self.browse_button = QPushButton(\"Browse...\")\n        self.browse_button.clicked.connect(self.browse_file)\n        file_layout.addWidget(file_label)\n        file_layout.addWidget(self.file_edit)\n        file_layout.addWidget(self.browse_button)\n        self.main_layout.addLayout(file_layout)\n\n        # --- Action Buttons ---\n        action_layout = QHBoxLayout()\n        self.load_button = QPushButton(\"Load EXIF Data\")\n        self.load_button.clicked.connect(self.start_load_task)\n        self.load_button.setEnabled(False)\n        self.save_button = QPushButton(\"Save Changes\")\n        self.save_button.clicked.connect(self.start_save_task)\n        self.save_button.setEnabled(False)\n        action_layout.addWidget(self.load_button)\n        action_layout.addWidget(self.save_button)\n        action_layout.addStretch()\n        self.main_layout.addLayout(action_layout)\n\n        # --- EXIF Data Display --- \n        self.exif_tree = QTreeWidget()\n        self.exif_tree.setColumnCount(3)\n        self.exif_tree.setHeaderLabels([\"IFD\", \"Tag Name\", \"Value\"])\n        self.exif_tree.header().setSectionResizeMode(COL_TAG_NAME, QHeaderView.Stretch)\n        self.exif_tree.header().setSectionResizeMode(COL_VALUE, QHeaderView.Stretch)\n        self.exif_tree.setAlternatingRowColors(True)\n        self.exif_tree.itemDoubleClicked.connect(self.edit_item)\n        self.main_layout.addWidget(self.exif_tree)\n\n        # --- Status Bar ---\n        self.status_bar = QStatusBar()\n        self.setStatusBar(self.status_bar)\n        self.status_bar.showMessage(\"Ready. Select an image file.\")\n\n        # Connect file path changes\n        self.file_edit.textChanged.connect(self.on_file_path_changed)\n\n    def browse_file(self):\n        filepath, _ = QFileDialog.getOpenFileName(self, \"Select Image File\", filter=\"Images (*.jpg *.jpeg *.tif *.tiff);;All Files (*)\")\n        if filepath:\n            self.file_edit.setText(filepath)\n            # Don't auto-load, require button press\n\n    def on_file_path_changed(self, filepath):\n        is_valid = os.path.isfile(filepath)\n        self.load_button.setEnabled(is_valid)\n        self.save_button.setEnabled(False) # Only enable after successful load\n        self.exif_tree.clear()\n        self.current_exif_data = {}\n        if is_valid:\n             self.status_bar.showMessage(f\"File selected: {filepath}. Click 'Load EXIF Data'.\")\n        else:\n             self.status_bar.showMessage(\"Ready. Select an image file.\")\n\n    def is_worker_running(self):\n         if self.thread and self.thread.isRunning():\n            QMessageBox.warning(self, \"Busy\", \"An operation is already in progress.\")\n            return True\n         return False\n         \n    def prepare_run(self):\n         self.status_bar.showMessage(\"Starting operation...\")\n         self.toggle_controls(False)\n         \n         self.thread = QThread()\n         self.worker = Worker(self.logic)\n         self.worker.moveToThread(self.thread)\n\n         # Connect worker signals\n         self.worker.exif_data_loaded.connect(self.display_exif_data)\n         self.worker.save_result.connect(self.on_save_result)\n         self.worker.error_occurred.connect(self.on_operation_error)\n         self.worker.finished.connect(self.on_task_finished)\n         self.thread.started.connect(self.worker.run)\n\n    # --- Task Start Methods ---\n    def start_load_task(self):\n        filepath = self.file_edit.text()\n        if not filepath or not os.path.isfile(filepath):\n            QMessageBox.warning(self, \"Invalid File\", \"Please select a valid image file first.\")\n            return\n        \n        if self.is_worker_running(): return\n        self.exif_tree.clear() # Clear previous data\n        self.current_exif_data = {}\n        self.prepare_run()\n        self.status_bar.showMessage(f\"Loading EXIF data from {os.path.basename(filepath)}...\")\n        self.worker.set_task('load', args=[filepath])\n        self.thread.start()\n\n    def start_save_task(self):\n        filepath = self.file_edit.text()\n        if not filepath or not os.path.isfile(filepath):\n            # Should not happen if save button is enabled, but check anyway\n            QMessageBox.warning(self, \"Invalid File\", \"Cannot save: File path is invalid.\")\n            return\n        if not self.current_exif_data:\n             QMessageBox.warning(self, \"No Data\", \"No EXIF data loaded or modified to save.\")\n             return\n             \n        # Update self.current_exif_data from the tree widget before saving\n        self.update_data_from_tree()\n\n        reply = QMessageBox.question(self, \"Confirm Save\", \
                                     f\"Save modified EXIF data back to:\\n{filepath}?\\nThis will modify the file directly.\",\n                                     QMessageBox.Yes | QMessageBox.Cancel, QMessageBox.Cancel)\n        if reply == QMessageBox.Cancel:\n             self.status_bar.showMessage(\"Save cancelled.\")\n             return\n             \n        if self.is_worker_running(): return\n        self.prepare_run()\n        self.status_bar.showMessage(f\"Saving EXIF data to {os.path.basename(filepath)}...\")\n        self.worker.set_task('save', args=[filepath, self.current_exif_data])\n        self.thread.start()\n\n    # --- UI Update Slots ---\n    def display_exif_data(self, loaded_data):\n        self.exif_tree.clear()\n        self.current_exif_data = loaded_data # Store the loaded data\n\n        if not loaded_data:\n            QMessageBox.information(self, \"No EXIF Data\", \"No EXIF data found in the selected file.\")\n            self.status_bar.showMessage(\"No EXIF data found.\")\n            self.save_button.setEnabled(False)\n            return\n\n        # Populate the tree widget\n        self.exif_tree.setUpdatesEnabled(False) # Performance for large data\n        for ifd_name, tags in loaded_data.items():\n            ifd_item = QTreeWidgetItem(self.exif_tree, [ifd_name, \"\", \"\"]) # Top-level item for IFD\n            ifd_item.setFlags(ifd_item.flags() & ~Qt.ItemIsEditable) # Make IFD name not editable\n            for tag_code, tag_info in tags.items():\n                tag_item = QTreeWidgetItem(ifd_item) # Child item for the tag\n                tag_item.setText(COL_IFD, \"\") # Leave IFD column blank for children\n                tag_item.setText(COL_TAG_NAME, f\"{tag_info['name']} ({tag_code})\")\n                tag_item.setText(COL_VALUE, str(tag_info['value'])) # Ensure value is string for display\n                # Store data needed for editing/saving in the item itself\n                tag_item.setData(COL_TAG_NAME, Qt.UserRole, tag_code) \n                tag_item.setData(COL_IFD, Qt.UserRole, ifd_name) \n                # Mark the value column as editable\n                tag_item.setFlags(tag_item.flags() | Qt.ItemIsEditable)\n                # Mark tag name as not editable\n                tag_item.setFlags(tag_item.flags() & ~Qt.ItemIsSelectable) # Deselect non-editable columns if needed? \n                \n            ifd_item.setExpanded(True) # Expand IFDs by default\n        self.exif_tree.setUpdatesEnabled(True)\n\n        self.status_bar.showMessage(\"EXIF data loaded successfully. Double-click value to edit.\")\n        self.save_button.setEnabled(True)\n\n    def edit_item(self, item, column):\n        \"\"\"Handle double-click to edit item value.\"\"\"\n        if column == COL_VALUE:\n            # Check if it's a tag item (has parent) and not the IFD item itself\n            if item.parent():\n                self.exif_tree.editItem(item, column)\n            # Else: it's an IFD item, do nothing or handle differently if needed\n\n    def update_data_from_tree(self):\n        \"\"\"Updates self.current_exif_data based on values currently in the tree.\"\"\"\n        # This is crucial before saving to capture any edits made via editItem\n        if not self.current_exif_data: return # No data loaded\n        \n        root = self.exif_tree.invisibleRootItem()\n        for i in range(root.childCount()): # Iterate through IFD items\n            ifd_item = root.child(i)\n            ifd_name = ifd_item.text(COL_IFD)\n            if ifd_name not in self.current_exif_data: continue # Should not happen\n                \n            for j in range(ifd_item.childCount()): # Iterate through tag items\n                tag_item = ifd_item.child(j)\n                tag_code = tag_item.data(COL_TAG_NAME, Qt.UserRole)\n                current_tree_value = tag_item.text(COL_VALUE)\n                \n                if tag_code in self.current_exif_data[ifd_name]:\n                    # Update the value in our stored dictionary\n                    self.current_exif_data[ifd_name][tag_code]['value'] = current_tree_value\n                # Else: tag somehow appeared in tree but not data? Ignore.\n\n    def on_save_result(self, success, message):\n        if success:\n            self.status_bar.showMessage(message, 5000)\n            QMessageBox.information(self, \"Save Successful\", message)\n            # Reload data after save to show potentially cleaned/formatted values from piexif?\n            self.start_load_task() # Automatically reload after save\n        else:\n            self.status_bar.showMessage(f\"Save Failed: {message}\")\n            QMessageBox.critical(self, \"Save Failed\", message)\n        # Re-enabling controls handled by on_task_finished\n\n    def on_operation_error(self, message):\n        self.status_bar.showMessage(f\"Error: {message}\")\n        QMessageBox.warning(self, \"Operation Error\", message)\n        self.save_button.setEnabled(False) # Disable save if load failed\n        # Re-enabling controls handled by on_task_finished\n\n    def on_task_finished(self):\n        if self.thread is None: return # Safety\n        \n        self.thread.quit()\n        self.thread.wait()\n        self.thread = None\n        self.worker = None\n        self.toggle_controls(True)\n        # Keep final status message unless it was just 'starting'\n        if self.status_bar.currentMessage() == \"Starting operation...\":\n             self.status_bar.showMessage(\"Ready\", 3000)\n\n    def toggle_controls(self, enable):\n        self.browse_button.setEnabled(enable)\n        # Load button depends on file selection AND controls enabled\n        self.load_button.setEnabled(enable and bool(self.file_edit.text()))\n        # Save button depends on successful load AND controls enabled\n        self.save_button.setEnabled(enable and bool(self.current_exif_data))\n        self.exif_tree.setEnabled(enable)\n\n    def closeEvent(self, event):\n        # Prompt if changes might be unsaved? Complex to track perfectly.\n        # Simple approach: If worker running, ask to stop.\n        if self.thread is not None and self.thread.isRunning():\n            reply = QMessageBox.question(self, \"Confirm Exit\", \
                                         \"An operation is in progress. Stop it and exit?\",\n                                         QMessageBox.Yes | QMessageBox.No, QMessageBox.No)\n            if reply == QMessageBox.Yes:\n                # No explicit stop needed for logic (fast ops), just quit thread\n                self.thread.quit()\n                self.thread.wait(500) \n                event.accept()\n            else:\
                event.ignore()\n        else:\
            event.accept()\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    main_win = ExifEditorApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "main.py",
    "content": "import sys\nfrom PyQt5.QtWidgets import QApplication\nfrom PyQt5.QtCore import Qt\nfrom exif_editor_gui import ExifEditorApp\n\nif __name__ == '__main__':\n    # Enable high DPI scaling for better visuals on some systems\n    if hasattr(QApplication, 'setAttribute'):\n        QApplication.setAttribute(Qt.AA_EnableHighDpiScaling, True)\n        QApplication.setAttribute(Qt.AA_UseHighDpiPixmaps, True)\n\n    app = QApplication(sys.argv)\n    main_win = ExifEditorApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "README.md",
    "content": "# EXIF Metadata Editor\n\nA Python tool with a PyQt5 GUI to view and edit EXIF (Exchangeable image file format) metadata in JPEG and TIFF image files.\n\n## Features\n\n*   **View EXIF Data:** Load JPEG or TIFF images and display their existing EXIF metadata in a structured tree view, grouped by IFD (Image File Directory, e.g., 0th, Exif, GPS).\n*   **Edit EXIF Tags:** Modify the values of existing EXIF tags directly within the tree view.\n*   **Human-Readable Tags:** Displays tag codes alongside their common names (e.g., `DateTimeOriginal (36867)`).\n*   **Save Changes:** Save the modified EXIF data back to the original image file.\n*   **User Interface:** Simple GUI built with PyQt5.\n*   **Responsiveness:** Uses a background thread for file I/O (loading/saving) to keep the GUI responsive.\n*   **Error Handling:** Reports issues like file not found, unsupported formats, invalid data, or permission errors.\n\n**Note:** This tool uses the `piexif` library. It aims to allow editing of common tag types but might have limitations with complex or custom tag structures. Saving modifies the original file directly, so ensure you have backups if needed.\n\n## Requirements\n\n*   Python 3.x\n*   PyQt5 (`pip install PyQt5`)\n*   Piexif (`pip install piexif`)\n*   Pillow (`pip install Pillow`) - Required by Piexif for some operations.\n\n## How to Run\n\n1.  Save the following files in the same directory:\n    *   `main.py`\n    *   `exif_editor_gui.py`\n    *   `exif_editor_logic.py`\n    *   `README.md` (this file)\n2.  Install the required libraries:\n    ```bash\n    pip install PyQt5 piexif Pillow\n    ```\n3.  Run the main script:\n    ```bash\n    python main.py\n    ```\n\n## How to Use\n\n1.  **Select Image:** Click the **Browse...** button and choose the JPEG or TIFF image file you want to inspect or modify.\n2.  **Load Data:** Click the **Load EXIF Data** button. The tool will read the file and display any found EXIF tags in the tree view below.\n3.  **View Data:** Expand the IFD nodes (like \"0th\", \"Exif\", \"GPS\") to see the tags within each directory. The columns show the IFD, Tag Name (and code), and the current Value.\n4.  **Edit Value:** Double-click on a value in the **Value** column for the tag you wish to change. Edit the text directly in the tree.\n    *   Enter values appropriate for the tag type (e.g., text for `Artist`, numbers for dimensions, `YYYY:MM:DD HH:MM:SS` for date/time tags, `Numerator/Denominator` for rational values like exposure).\n    *   Parsing attempts to convert your input back to the correct internal format, but invalid input may cause errors on saving.\n5.  **Save Changes:** Once you have made your edits, click the **Save Changes** button.\n6.  **Confirm:** You will be asked to confirm that you want to modify the original file.\n7.  **Check Status:** The status bar will report whether the save was successful or if any errors occurred. If successful, the EXIF data will be automatically reloaded to show the saved values.\n"
  }
]
