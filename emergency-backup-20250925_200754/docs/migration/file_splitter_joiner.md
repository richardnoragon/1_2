[
  {
    "filename": "file_op_logic.py",
    "content": "import os\nimport math\nimport json\nfrom PyQt5.QtCore import QObject, pyqtSignal\n\nCHUNK_RW_SIZE = 1024 * 1024 # 1MB read/write buffer\nMETADATA_FILENAME = \"_metadata.json\"\n\nclass FileOperationLogic(QObject):\n    \"\"\"Handles the logic for splitting and joining files.\"\"\"\n    progress_updated = pyqtSignal(int, int, str) # value, total, message\n    operation_complete = pyqtSignal(str)         # success message\n    error_occurred = pyqtSignal(str)           # error message\n    finished = pyqtSignal()                      # signals thread completion\n\n    def __init__(self):\n        super().__init__()\n        self._is_running = False\n\n    def stop(self):\n        self.progress_updated.emit(0, 1, \"Stopping operation...\")\n        self._is_running = False\n\n    def _calculate_split_params(self, file_size, split_mode, value, unit_multiplier=1):\n        \"\"\"Calculates chunk size and number of chunks based on user input.\"\"\"\n        if split_mode == 'size':\n            chunk_size = int(value * unit_multiplier)\n            if chunk_size <= 0:\n                raise ValueError(\"Chunk size must be positive.\")\n            if chunk_size >= file_size and file_size > 0:\n                 # If chunk size >= file size, create only one chunk unless file is empty\n                 num_chunks = 1\n                 chunk_size = file_size \n            elif file_size == 0:\n                 num_chunks = 0\n                 chunk_size = 0\n            else:\n                 num_chunks = math.ceil(file_size / chunk_size)\n        elif split_mode == 'parts':\n            num_chunks = int(value)\n            if num_chunks <= 0:\n                raise ValueError(\"Number of parts must be positive.\")\n            if file_size == 0:\n                chunk_size = 0\n                num_chunks = 0\n            else:\n                 # Calculate chunk size, ensure last chunk handles remainder\n                 chunk_size = file_size // num_chunks \n                 # Adjust if division is perfect vs imperfect for accurate progress later\n                 # Actually, simpler to just read up to chunk_size each time\n                 # and let the last chunk be smaller.\n                 # Recalculate chunk_size for avg/reporting, actual read handles remainder\n                 chunk_size = math.ceil(file_size / num_chunks)\n        else:\n            raise ValueError(f\"Invalid split mode: {split_mode}\")\n            \n        if num_chunks > 9999: # Limit padding practicalities\n              raise ValueError(\"Too many chunks requested (max 9999).\")\n              \n        return chunk_size, num_chunks\n\n    def split_file(self, input_filepath, output_dir, split_mode, value, unit_multiplier=1):\n        \"\"\"Splits the input file into smaller chunks.\"\"\"\n        self._is_running = True\n        try:\n            if not os.path.exists(input_filepath):\n                raise FileNotFoundError(f\"Input file not found: {input_filepath}\")\n            if not os.path.isfile(input_filepath):\n                 raise ValueError(f\"Input path is not a file: {input_filepath}\")\n            if not os.path.exists(output_dir):\n                os.makedirs(output_dir)\n            elif not os.path.isdir(output_dir):\n                 raise ValueError(f\"Output path is not a directory: {output_dir}\")\n\n            file_size = os.path.getsize(input_filepath)\n            base_filename = os.path.basename(input_filepath)\n\n            if file_size == 0:\n                self.operation_complete.emit(\"Input file is empty. No chunks created.\")\n                self._is_running = False\n                self.finished.emit()\n                return\n\n            chunk_size, num_chunks = self._calculate_split_params(file_size, split_mode, value, unit_multiplier)\n            \n            if num_chunks == 0: # Should only happen for zero size file, handled above\n                 self.operation_complete.emit(\"No chunks needed.\")\n                 self._is_running = False\n                 self.finished.emit()\n                 return\n\n            # Determine padding length (e.g., 3 for up to 999 chunks -> .part001)\n            padding = max(3, len(str(num_chunks)))\n            chunk_pattern = f\"{base_filename}.part{{:0{padding}d}}\"\n\n            self.progress_updated.emit(0, num_chunks, f\"Starting split: {num_chunks} chunks, size ~{chunk_size} bytes...\")\n\n            bytes_written_total = 0\n            chunks_created = 0\n            with open(input_filepath, 'rb') as infile:\n                for i in range(num_chunks):\n                    if not self._is_running: break\n                    chunk_num = i + 1\n                    chunk_filename = chunk_pattern.format(chunk_num)\n                    chunk_filepath = os.path.join(output_dir, chunk_filename)\n                    self.progress_updated.emit(chunks_created, num_chunks, f\"Writing chunk {chunk_num}/{num_chunks}: {chunk_filename}\")\n\n                    bytes_written_this_chunk = 0\n                    with open(chunk_filepath, 'wb') as outfile:\n                        # Read and write in smaller blocks within the larger chunk target\n                        while bytes_written_this_chunk < chunk_size:\n                            if not self._is_running: break\n                            # Calculate remaining needed for this chunk\n                            remaining_in_chunk = chunk_size - bytes_written_this_chunk\n                            # Determine read size: min of buffer, remaining in chunk, remaining in file\n                            read_size = min(CHUNK_RW_SIZE, remaining_in_chunk, file_size - bytes_written_total)\n                            \n                            if read_size <= 0: break # No more data left in file or chunk full\n                                \n                            data = infile.read(read_size)\n                            if not data: break # End of input file\n                                \n                            outfile.write(data)\n                            bytes_written_this_chunk += len(data)\n                            bytes_written_total += len(data)\n                        \n                    if not self._is_running: break\n                    chunks_created += 1\n                    self.progress_updated.emit(chunks_created, num_chunks, f\"Finished chunk {chunk_num}/{num_chunks}\")\n            \n            if not self._is_running:\n                 # Clean up partially written chunks? Optional.\n                 self.error_occurred.emit(\"Split operation cancelled.\")\n            else:\n                 # Create metadata file\n                 metadata = {\n                     'original_filename': base_filename,\n                     'total_size': file_size,\n                     'num_chunks': num_chunks,\n                     'chunk_size': chunk_size, # Note: This might be approximate if split by parts\n                     'chunk_pattern': chunk_pattern,\n                     'padding': padding\n                 }\n                 meta_filepath = os.path.join(output_dir, METADATA_FILENAME)\n                 try:\n                      with open(meta_filepath, 'w') as metafile:\n                          json.dump(metadata, metafile, indent=4)\n                 except Exception as e:\n                      self.error_occurred.emit(f\"Warning: Could not write metadata file: {e}\")\n                      \n                 self.operation_complete.emit(f\"File successfully split into {chunks_created} chunks in {output_dir}.\")\n\n        except (FileNotFoundError, ValueError) as e:\n            self.error_occurred.emit(f\"Error: {e}\")\n        except PermissionError as e:\n             self.error_occurred.emit(f\"Permission Error: {e}. Check file/directory permissions.\")\n        except OSError as e:\n             self.error_occurred.emit(f\"Disk Error: {e}. Check available disk space.\")\n        except Exception as e:\n            self.error_occurred.emit(f\"An unexpected error occurred during split: {e}\")\n        finally:\n            self._is_running = False\n            self.finished.emit()\n\n    def join_files(self, first_chunk_path, output_filepath):\n        \"\"\"Joins file chunks back into a single file.\"\"\"\n        self._is_running = True\n        try:\n            if not os.path.exists(first_chunk_path):\n                raise FileNotFoundError(f\"First chunk file not found: {first_chunk_path}\")\n            if not os.path.isfile(first_chunk_path):\n                 raise ValueError(f\"Selected path is not a file: {first_chunk_path}\")\n                 \n            chunk_dir = os.path.dirname(first_chunk_path)\n            chunk_basename = os.path.basename(first_chunk_path)\n\n            # Try to load metadata first\n            metadata = None\n            meta_filepath = os.path.join(chunk_dir, METADATA_FILENAME)\n            if os.path.exists(meta_filepath):\n                try:\n                    with open(meta_filepath, 'r') as f:\n                        metadata = json.load(f)\n                        self.progress_updated.emit(0, 1, f\"Loaded metadata for {metadata['original_filename']}\")\n                except Exception as e:\n                    self.progress_updated.emit(0, 1, f\"Warning: Could not read metadata file ({e}), attempting manual join.\")\n                    metadata = None # Reset on failure\n            \n            # Determine join parameters\n            if metadata:\n                 num_chunks = metadata['num_chunks']\n                 chunk_pattern = metadata['chunk_pattern']\n                 padding = metadata['padding']\n                 expected_total_size = metadata.get('total_size') # Might not exist in old metadata\n            else:\n                 # Infer from first chunk name (e.g., file.part001)\n                 parts = chunk_basename.split('.part')\n                 if len(parts) != 2 or not parts[1].isdigit():\n                      raise ValueError(\"Cannot infer chunk sequence from filename. Expected format like 'filename.partXXX'.\")\n                 base_filename = parts[0]\n                 padding = len(parts[1])\n                 chunk_pattern = f\"{base_filename}.part{{:0{padding}d}}\"\n                 # Count chunks manually (less reliable)\n                 num_chunks = 0\n                 for i in range(1, 10000): # Max check\n                     check_path = os.path.join(chunk_dir, chunk_pattern.format(i))\n                     if os.path.exists(check_path):\n                         num_chunks += 1\n                     else:\n                         break\n                 if num_chunks == 0:\n                     raise ValueError(\"Could not find any valid chunks to join.\")\n                 expected_total_size = None # Cannot know for sure without metadata\n                 self.progress_updated.emit(0, num_chunks, f\"Found {num_chunks} potential chunks based on pattern.\")\n\n            # Start joining\n            output_dir = os.path.dirname(output_filepath)\n            if output_dir and not os.path.exists(output_dir):\n                 os.makedirs(output_dir)\n                 \n            self.progress_updated.emit(0, num_chunks, f\"Starting join operation...\")\n            bytes_written_total = 0\n            chunks_processed = 0\n            \n            with open(output_filepath, 'wb') as outfile:\n                 for i in range(num_chunks):\n                     if not self._is_running: break\n                     chunk_num = i + 1\n                     chunk_filename = chunk_pattern.format(chunk_num)\n                     chunk_filepath = os.path.join(chunk_dir, chunk_filename)\n                     \n                     if not os.path.exists(chunk_filepath):\n                          raise FileNotFoundError(f\"Missing chunk: {chunk_filename}\")\n                          \n                     self.progress_updated.emit(chunks_processed, num_chunks, f\"Reading chunk {chunk_num}/{num_chunks}: {chunk_filename}\")\n                     \n                     try:\n                         with open(chunk_filepath, 'rb') as infile:\n                             while self._is_running:\n                                 data = infile.read(CHUNK_RW_SIZE)\n                                 if not data: break\n                                 outfile.write(data)\n                                 bytes_written_total += len(data)\n                     except Exception as e:\n                          raise IOError(f\"Error reading chunk {chunk_filename}: {e}\") from e\n                          \n                     if not self._is_running: break\n                     chunks_processed += 1\n                     self.progress_updated.emit(chunks_processed, num_chunks, f\"Finished chunk {chunk_num}/{num_chunks}\")\n\n            if not self._is_running:\n                 # Clean up partially written output file? Optional.\n                 if os.path.exists(output_filepath):\n                      try: os.remove(output_filepath)\n                      except: pass\n                 self.error_occurred.emit(\"Join operation cancelled.\")\n            else:\n                 # Final Verification (optional)\n                 final_size = os.path.getsize(output_filepath)\n                 verification_msg = \"\"\n                 if expected_total_size is not None:\n                     if final_size == expected_total_size:\n                         verification_msg = f\" Final size ({final_size} bytes) matches expected size.\" \n                     else:\n                         verification_msg = f\" WARNING: Final size ({final_size} bytes) does NOT match expected size ({expected_total_size} bytes)!\" \n                         \n                 self.operation_complete.emit(f\"Successfully joined {chunks_processed} chunks into {output_filepath}.{verification_msg}\")\n\n        except (FileNotFoundError, ValueError, IOError) as e:\n            self.error_occurred.emit(f\"Error: {e}\")\n        except PermissionError as e:\n             self.error_occurred.emit(f\"Permission Error: {e}. Check file/directory permissions.\")\n        except OSError as e:\n             self.error_occurred.emit(f\"Disk Error: {e}. Check available disk space.\")\n        except Exception as e:\n            self.error_occurred.emit(f\"An unexpected error occurred during join: {e}\")\n        finally:\n            self._is_running = False\n            self.finished.emit()\n"
  },
  {
    "filename": "splitter_joiner_gui.py",
    "content": "import sys\nimport os\nfrom PyQt5.QtWidgets import (\n    QApplication, QMainWindow, QWidget, QVBoxLayout, QHBoxLayout, QTabWidget,\n    QPushButton, QLabel, QLineEdit, QFileDialog, QStatusBar, QMessageBox,\n    QProgressBar, QSpinBox, QComboBox, QRadioButton, QGroupBox, QSizePolicy\n)\nfrom PyQt5.QtCore import Qt, QThread, pyqtSignal, QObject\n\nfrom file_op_logic import FileOperationLogic\n\nclass Worker(QObject):\n    \"\"\"Worker thread for file operations.\"\"\"\n    progress_updated = pyqtSignal(int, int, str)\n    operation_complete = pyqtSignal(str)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, logic_instance):\n        super().__init__()\n        self.logic = logic_instance\n        self._mode = None # 'split' or 'join'\n        self._args = []\n        self._kwargs = {}\n\n    def run(self):\n        # Connect logic signals\n        self.logic.progress_updated.connect(self.progress_updated)\n        self.logic.operation_complete.connect(self.operation_complete)\n        self.logic.error_occurred.connect(self.error_occurred)\n        self.logic.finished.connect(self.on_logic_finished)\n\n        try:\n            if self._mode == 'split':\n                self.logic.split_file(*self._args, **self._kwargs)\n            elif self._mode == 'join':\n                self.logic.join_files(*self._args, **self._kwargs)\n            else:\n                self.error_occurred.emit(\"Worker started with invalid mode.\")\n                self.on_logic_finished()\n        except Exception as e:\n             self.error_occurred.emit(f\"Critical error in worker execution: {e}\")\n             self.on_logic_finished()\n\n    def set_task(self, mode, args=None, kwargs=None):\n        self._mode = mode\n        self._args = args if args is not None else []\n        self._kwargs = kwargs if kwargs is not None else {}\n\n    def stop(self):\n        self.logic.stop()\n\n    def on_logic_finished(self):\n        # Disconnect signals\n        try:\n            self.logic.progress_updated.disconnect(self.progress_updated)\n            self.logic.operation_complete.disconnect(self.operation_complete)\n            self.logic.error_occurred.disconnect(self.error_occurred)\n            self.logic.finished.disconnect(self.on_logic_finished)\n        except TypeError:\n            pass # Ignore if already disconnected\n        self.finished.emit()\n\nclass FileSplitterJoinerApp(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle(\"File Splitter/Joiner\")\n        self.setGeometry(100, 100, 600, 450)\n\n        self.central_widget = QWidget()\n        self.setCentralWidget(self.central_widget)\n        self.main_layout = QVBoxLayout(self.central_widget)\n\n        # --- Worker Thread Setup ---\n        self.thread = None\n        self.worker = None\n        self.logic = FileOperationLogic()\n\n        # --- Tabs ---\n        self.tabs = QTabWidget()\n        self.main_layout.addWidget(self.tabs)\n\n        self.create_split_tab()\n        self.create_join_tab()\n\n        # --- Progress Bar and Status Bar ---\n        self.progress_bar = QProgressBar()\n        self.progress_bar.setTextVisible(True)\n        self.progress_bar.setValue(0)\n        self.main_layout.addWidget(self.progress_bar)\n\n        self.status_bar = QStatusBar()\n        self.setStatusBar(self.status_bar)\n        self.status_bar.showMessage(\"Ready\")\n\n        # --- Stop Button ---\n        self.stop_button = QPushButton(\"Stop Operation\")\n        self.stop_button.setEnabled(False)\n        self.stop_button.clicked.connect(self.stop_task)\n        self.main_layout.addWidget(self.stop_button)\n\n    def create_split_tab(self):\n        self.split_tab = QWidget()\n        layout = QVBoxLayout(self.split_tab)\n\n        # Input File\n        in_layout = QHBoxLayout()\n        in_label = QLabel(\"File to Split:\")\n        self.split_infile_edit = QLineEdit()\n        self.split_infile_edit.setReadOnly(True)\n        self.split_browse_in_button = QPushButton(\"Browse...\")\n        self.split_browse_in_button.clicked.connect(self.browse_split_input)\n        in_layout.addWidget(in_label)\n        in_layout.addWidget(self.split_infile_edit)\n        in_layout.addWidget(self.split_browse_in_button)\n        layout.addLayout(in_layout)\n\n        # Output Directory\n        out_layout = QHBoxLayout()\n        out_label = QLabel(\"Output Directory:\")\n        self.split_outdir_edit = QLineEdit()\n        self.split_outdir_edit.setReadOnly(True)\n        self.split_browse_out_button = QPushButton(\"Browse...\")\n        self.split_browse_out_button.clicked.connect(self.browse_split_output)\n        out_layout.addWidget(out_label)\n        out_layout.addWidget(self.split_outdir_edit)\n        out_layout.addWidget(self.split_browse_out_button)\n        layout.addLayout(out_layout)\n\n        # Splitting Options\n        options_group = QGroupBox(\"Splitting Options\")\n        options_layout = QVBoxLayout()\n\n        self.split_rb_size = QRadioButton(\"Split by Size:\")\n        size_layout = QHBoxLayout()\n        self.split_size_spinbox = QSpinBox()\n        self.split_size_spinbox.setRange(1, 1024 * 10) # Max 10GB chunks initially\n        self.split_size_spinbox.setValue(100)\n        self.split_unit_combo = QComboBox()\n        self.split_unit_combo.addItems([\"MB\", \"KB\", \"GB\"])\n        self.split_unit_combo.setCurrentText(\"MB\")\n        size_layout.addWidget(self.split_rb_size)\n        size_layout.addWidget(self.split_size_spinbox)\n        size_layout.addWidget(self.split_unit_combo)\n        size_layout.addStretch()\n        options_layout.addLayout(size_layout)\n\n        self.split_rb_parts = QRadioButton(\"Split into Parts:\")\n        parts_layout = QHBoxLayout()\n        self.split_parts_spinbox = QSpinBox()\n        self.split_parts_spinbox.setRange(2, 9999) # Min 2 parts\n        self.split_parts_spinbox.setValue(2)\n        self.split_parts_spinbox.setEnabled(False)\n        parts_layout.addWidget(self.split_rb_parts)\n        parts_layout.addWidget(self.split_parts_spinbox)\n        parts_layout.addStretch()\n        parts_layout.addWidget(QLabel(\"parts\"))\n        options_layout.addLayout(parts_layout)\n        \n        options_group.setLayout(options_layout)\n        layout.addWidget(options_group)\n        \n        # Connect radio buttons\n        self.split_rb_size.setChecked(True)\n        self.split_rb_size.toggled.connect(self.toggle_split_options)\n        self.split_rb_parts.toggled.connect(self.toggle_split_options)\n\n        # Split Button\n        self.split_button = QPushButton(\"Split File\")\n        self.split_button.clicked.connect(self.start_split_task)\n        layout.addWidget(self.split_button)\n        layout.addStretch()\n\n        self.tabs.addTab(self.split_tab, \"Split File\")\n\n    def create_join_tab(self):\n        self.join_tab = QWidget()\n        layout = QVBoxLayout(self.join_tab)\n\n        # Input First Chunk\n        in_layout = QHBoxLayout()\n        in_label = QLabel(\"First Chunk File (.part001):\")\n        self.join_infile_edit = QLineEdit()\n        self.join_infile_edit.setReadOnly(True)\n        self.join_browse_in_button = QPushButton(\"Browse...\")\n        self.join_browse_in_button.clicked.connect(self.browse_join_input)\n        in_layout.addWidget(in_label)\n        in_layout.addWidget(self.join_infile_edit)\n        in_layout.addWidget(self.join_browse_in_button)\n        layout.addLayout(in_layout)\n\n        # Output Joined File\n        out_layout = QHBoxLayout()\n        out_label = QLabel(\"Save Joined File As:\")\n        self.join_outfile_edit = QLineEdit()\n        self.join_outfile_edit.setReadOnly(True)\n        self.join_browse_out_button = QPushButton(\"Save As...\")\n        self.join_browse_out_button.clicked.connect(self.browse_join_output)\n        out_layout.addWidget(out_label)\n        out_layout.addWidget(self.join_outfile_edit)\n        out_layout.addWidget(self.join_browse_out_button)\n        layout.addLayout(out_layout)\n\n        # Join Button\n        self.join_button = QPushButton(\"Join Files\")\n        self.join_button.clicked.connect(self.start_join_task)\n        layout.addWidget(self.join_button)\n        layout.addStretch()\n\n        self.tabs.addTab(self.join_tab, \"Join Files\")\n        \n    def toggle_split_options(self):\n        is_size_mode = self.split_rb_size.isChecked()\n        self.split_size_spinbox.setEnabled(is_size_mode)\n        self.split_unit_combo.setEnabled(is_size_mode)\n        self.split_parts_spinbox.setEnabled(not is_size_mode)\n\n    # --- Browse Methods ---\n    def browse_split_input(self):\n        filepath, _ = QFileDialog.getOpenFileName(self, \"Select File to Split\")\n        if filepath:\n            self.split_infile_edit.setText(filepath)\n            # Suggest output dir based on input file location\n            self.split_outdir_edit.setText(os.path.dirname(filepath))\n\n    def browse_split_output(self):\n        dirpath = QFileDialog.getExistingDirectory(self, \"Select Output Directory for Chunks\")\n        if dirpath:\n            self.split_outdir_edit.setText(dirpath)\n\n    def browse_join_input(self):\n        # Look for .part001 or similar, or metadata file\n        filepath, _ = QFileDialog.getOpenFileName(self, \n                                                \"Select First Chunk File (.partXXX) or Metadata File\",\n                                                filter=\"Chunk Files (*.part*);;Metadata File (*_metadata.json);;All Files (*)\")\n        if filepath:\n            self.join_infile_edit.setText(filepath)\n            # Suggest output filename based on chunk name or metadata\n            base_dir = os.path.dirname(filepath)\n            suggested_name = \"joined_file\"\n            if filepath.endswith(\"_metadata.json\"):\n                 try:\n                     with open(filepath, 'r') as f:\n                         meta = json.load(f)\n                         suggested_name = meta.get('original_filename', suggested_name)\n                 except Exception:\
                     pass # Ignore errors reading metadata here\n            elif \".part\" in os.path.basename(filepath):\n                 suggested_name = os.path.basename(filepath).split('.part')[0]\n                 \n            self.join_outfile_edit.setText(os.path.join(base_dir, suggested_name))\n\n    def browse_join_output(self):\n        # Suggest name based on input chunk if available\n        default_name = os.path.basename(self.join_outfile_edit.text()) \
                         if self.join_outfile_edit.text() else \"joined_file\"\n        default_dir = os.path.dirname(self.join_infile_edit.text()) \
                        if self.join_infile_edit.text() else \"\"\n        filepath, _ = QFileDialog.getSaveFileName(self, \"Save Joined File As\", \
                                                   directory=os.path.join(default_dir, default_name))\n        if filepath:\n            self.join_outfile_edit.setText(filepath)\n\n    # --- Task Start Methods ---\n    def start_split_task(self):\n        infile = self.split_infile_edit.text()\n        outdir = self.split_outdir_edit.text()\n\n        if not infile or not os.path.isfile(infile):\n            QMessageBox.warning(self, \"Input Error\", \"Please select a valid file to split.\")\n            return\n        if not outdir or not os.path.isdir(outdir):\n             QMessageBox.warning(self, \"Output Error\", \"Please select a valid output directory.\")\n             return\n\n        split_mode = 'size' if self.split_rb_size.isChecked() else 'parts'\n        value = 0\n        unit_multiplier = 1\n        if split_mode == 'size':\n            value = self.split_size_spinbox.value()\n            unit = self.split_unit_combo.currentText()\n            if unit == 'KB': unit_multiplier = 1024\n            elif unit == 'MB': unit_multiplier = 1024 * 1024\n            elif unit == 'GB': unit_multiplier = 1024 * 1024 * 1024\n        else:\n            value = self.split_parts_spinbox.value()\n        \n        if self.is_worker_running(): return\n        self.prepare_run()\n        self.worker.set_task('split', args=[infile, outdir, split_mode, value], kwargs={'unit_multiplier': unit_multiplier})\n        self.thread.start()\n\n    def start_join_task(self):\n        infile = self.join_infile_edit.text() # First chunk or metadata\n        outfile = self.join_outfile_edit.text()\n\n        if not infile or not os.path.isfile(infile):\n            QMessageBox.warning(self, \"Input Error\", \"Please select the first chunk file (.partXXX) or the _metadata.json file.\")\n            return\n        if not outfile:\n             QMessageBox.warning(self, \"Output Error\", \"Please specify where to save the joined file.\")\n             return\n        if os.path.abspath(infile) == os.path.abspath(outfile): # Prevent overwriting input\n             QMessageBox.warning(self, \"Output Error\", \"Output file cannot be the same as an input chunk file.\")\n             return\n             \n        # Handle metadata file selection - logic needs the *first chunk*\n        first_chunk_path_to_use = infile\n        if infile.endswith(\"_metadata.json\"):\n             # Try to find the first actual chunk from metadata\n             try:\n                  with open(infile, 'r') as f:\n                       meta = json.load(f)\n                       chunk_pattern = meta['chunk_pattern']\n                       first_chunk_name = chunk_pattern.format(1)\n                       potential_path = os.path.join(os.path.dirname(infile), first_chunk_name)\n                       if os.path.exists(potential_path):\n                            first_chunk_path_to_use = potential_path\n                       else:\
                            raise FileNotFoundError(f\"First chunk '{first_chunk_name}' indicated by metadata not found.\")\n             except Exception as e:\n                  QMessageBox.critical(self, \"Metadata Error\", f\"Could not process metadata file or find first chunk: {e}\")\n                  return\n\n        if self.is_worker_running(): return\n        self.prepare_run()\n        self.worker.set_task('join', args=[first_chunk_path_to_use, outfile])\n        self.thread.start()\n        \n    def is_worker_running(self):\n         if self.thread and self.thread.isRunning():\n            QMessageBox.warning(self, \"Busy\", \"An operation is already in progress.\")\n            return True\n         return False\n         \n    def prepare_run(self):\n         self.status_bar.showMessage(\"Starting operation...\")\n         self.progress_bar.setValue(0)\n         self.progress_bar.setFormat(\"Starting...\")\n         self.toggle_controls(False)\n         \n         self.thread = QThread()\n         self.worker = Worker(self.logic)\n         self.worker.moveToThread(self.thread)\n\n         # Connect worker signals\n         self.worker.progress_updated.connect(self.update_progress)\n         self.worker.operation_complete.connect(self.on_operation_complete)\n         self.worker.error_occurred.connect(self.on_operation_error)\n         self.worker.finished.connect(self.on_task_finished)\n         self.thread.started.connect(self.worker.run)\n\n    # --- UI Update Slots ---\n    def update_progress(self, value, total, message):\n        self.status_bar.showMessage(message)\n        if total > 0:\n             self.progress_bar.setRange(0, total)\n             self.progress_bar.setValue(value)\n             percent = int((value / total) * 100) if total > 0 else 0\n             self.progress_bar.setFormat(f\"{message} - {percent}%\")\n        else:\n             self.progress_bar.setRange(0, 0) # Indeterminate\n             self.progress_bar.setFormat(message)\n\n    def on_operation_complete(self, message):\n        self.status_bar.showMessage(message, 10000) # Show for 10s\n        QMessageBox.information(self, \"Operation Successful\", message)\n        self.progress_bar.setRange(0,1)\n        self.progress_bar.setValue(1)\n        self.progress_bar.setFormat(\"Complete!\")\n        # Don't re-enable controls here, wait for finished signal\n\n    def on_operation_error(self, message):\n        self.status_bar.showMessage(f\"Error: {message}\")\n        QMessageBox.critical(self, \"Operation Failed\", message)\n        self.progress_bar.setRange(0,1)\n        self.progress_bar.setValue(0)\n        self.progress_bar.setFormat(\"Failed!\")\n        # Don't re-enable controls here, wait for finished signal\n\n    def on_task_finished(self):\n        if self.thread is None: return # Safety\n        \n        self.thread.quit()\n        self.thread.wait()\n        self.thread = None\n        self.worker = None\n        self.toggle_controls(True)\n        # Keep final status message\n\n    def stop_task(self):\n        if self.worker:\n             self.status_bar.showMessage(\"Attempting to stop...\")\n             self.worker.stop()\n             self.stop_button.setEnabled(False) # Disable stop button immediately\n\n    def toggle_controls(self, enable):\n        self.tabs.setEnabled(enable)\n        self.stop_button.setEnabled(not enable)\n        # Enable buttons based on tab state as well if needed later\n        self.split_button.setEnabled(enable)\n        self.join_button.setEnabled(enable)\n        self.split_browse_in_button.setEnabled(enable)\n        self.split_browse_out_button.setEnabled(enable)\n        self.join_browse_in_button.setEnabled(enable)\n        self.join_browse_out_button.setEnabled(enable)\n\n    def closeEvent(self, event):\n        if self.thread is not None and self.thread.isRunning():\n            reply = QMessageBox.question(self, \"Confirm Exit\", \
                                         \"An operation is in progress. Stop it and exit?\",\n                                         QMessageBox.Yes | QMessageBox.No, QMessageBox.No)\n            if reply == QMessageBox.Yes:\n                self.stop_task()\n                # Give a very short time for stop signal to potentially process\n                self.thread.quit()\n                self.thread.wait(500) \n                event.accept()\n            else:\
                event.ignore()\n        else:\
            event.accept()\n\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    main_win = FileSplitterJoinerApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "main.py",
    "content": "import sys\nfrom PyQt5.QtWidgets import QApplication\nfrom PyQt5.QtCore import Qt\nfrom splitter_joiner_gui import FileSplitterJoinerApp\n\nif __name__ == '__main__':\n    # Enable high DPI scaling for better visuals on some systems\n    if hasattr(QApplication, 'setAttribute'):\n        QApplication.setAttribute(Qt.AA_EnableHighDpiScaling, True)\n        QApplication.setAttribute(Qt.AA_UseHighDpiPixmaps, True)\n\n    app = QApplication(sys.argv)\n    main_win = FileSplitterJoinerApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "README.md",
    "content": "# File Splitter/Joiner\n\nA Python tool with a PyQt5 GUI to split large files into smaller, more manageable chunks and join them back together.\n\n## Features\n\n*   **Split Files:**\n    *   Select a large file to split.\n    *   Choose splitting method:\n        *   By specific chunk size (KB, MB, GB).\n        *   Into a specific number of parts.\n    *   Select an output directory for the generated chunks.\n    *   Creates numbered chunk files (e.g., `yourfile.part001`, `yourfile.part002`, ...).\n    *   Generates a `_metadata.json` file in the output directory containing information about the split (original filename, size, chunk count, etc.) to aid joining.\n*   **Join Files:**\n    *   Select the *first* chunk file (`*.part001`, etc.) or the `_metadata.json` file.\n    *   Specify the output path for the reconstructed file.\n    *   Automatically detects subsequent chunks based on naming convention or metadata.\n    *   Reassembles the chunks in the correct order.\n    *   Optionally verifies the final size against metadata if available.\n*   **User Interface:** Simple tabbed GUI (Split/Join) built with PyQt5.\n*   **Large File Handling:** Reads and writes files in chunks to minimize memory usage.\n*   **Responsiveness:** Uses a separate thread for file operations to keep the GUI responsive.\n*   **Progress Indication:** Shows progress via a progress bar and status messages.\n*   **Cancellation:** Allows stopping long-running split or join operations.\n*   **Error Handling:** Reports issues like file not found, permission errors, missing chunks, or disk space problems.\n\n## Requirements\n\n*   Python 3.x\n*   PyQt5 (`pip install PyQt5`)\n\n## How to Run\n\n1.  Save the following files in the same directory:\n    *   `main.py`\n    *   `splitter_joiner_gui.py`\n    *   `file_op_logic.py`\n    *   `README.md` (this file)\n2.  Install PyQt5 if you haven't already:\n    ```bash\n    pip install PyQt5\n    ```\n3.  Run the main script:\n    ```bash\n    python main.py\n    ```\n\n## How to Use\n\n### Splitting a File\n\n1.  Go to the **Split File** tab.\n2.  Click **Browse...** next to \"File to Split\" and select the large file you want to divide.\n3.  Click **Browse...** next to \"Output Directory\" and choose where the chunk files should be saved.\n4.  Under **Splitting Options**, choose either:\n    *   **Split by Size:** Enter a size value and select the unit (KB, MB, GB).\n    *   **Split into Parts:** Enter the desired number of chunks.\n5.  Click the **Split File** button.\n6.  Monitor the progress bar and status messages. You can click **Stop Operation** to cancel.\n7.  Once complete, the chunk files (e.g., `filename.part001`, `filename.part002`, ...) and a `_metadata.json` file will be in the specified output directory.\n\n### Joining Files\n\n1.  Go to the **Join Files** tab.\n2.  Click **Browse...** next to \"First Chunk File\". Navigate to the directory containing the chunks and select either:\n    *   The **first** chunk file (usually ending in `.part001`).\n    *   The `_metadata.json` file (recommended if it exists).\n3.  Click **Save As...** next to \"Save Joined File As\" and choose the location and name for the reconstructed file. **Ensure this is not the same path as one of the chunk files.**\n4.  Click the **Join Files** button.\n5.  Monitor the progress bar and status messages. You can click **Stop Operation** to cancel.\n6.  Once complete, the fully reassembled file will be saved at the location you specified.\n"
  }
]
