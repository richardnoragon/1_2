[
  {
    "filename": "disk_scan_logic.py",
    "content": "import os\nimport time\nfrom PyQt5.QtCore import QObject, pyqtSignal\n\nclass DiskScanLogic(QObject):\n    \"\"\"Scans a directory to calculate item sizes for treemap visualization.\"\"\"\n    progress_updated = pyqtSignal(str, int, int) # message, current, total\n    scan_complete = pyqtSignal(dict) # data for treemap: {'path': path, 'items': [{'name': name, 'size': size, 'path': item_path}, ...]}\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self):\n        super().__init__()\n        self._is_running = False\n        self._target_path = None\n\n    def stop(self):\n        self.progress_updated.emit(\"Stopping scan...\", 0, 0)\n        self._is_running = False\n\n    def start_scan(self, target_path):\n        \"\"\"Initiates the directory scan process.\"\"\"\n        if not os.path.isdir(target_path):\n            self.error_occurred.emit(f\"Error: Not a valid directory: {target_path}\")\n            self.finished.emit()\n            return\n\n        self._is_running = True\n        self._target_path = target_path\n        scan_data = {'path': target_path, 'items': []}\n        total_size = 0\n        items_processed = 0\n\n        try:\n            # --- First Pass: Count items for progress --- \n            # This can still be slow for huge dirs, might need optimization or indeterminate progress\n            all_entries = []\n            try:\n                 # Use scandir for potentially better performance than listdir + stat\n                 with os.scandir(target_path) as entries:\n                     for entry in entries:\n                         if not self._is_running: break\n                         all_entries.append(entry)\n            except PermissionError:\n                 self.error_occurred.emit(f\"Permission denied accessing: {target_path}\")\n                 self._is_running = False # Stop if initial access fails\n            except Exception as e:\n                 self.error_occurred.emit(f\"Error listing directory {target_path}: {e}\")\n                 self._is_running = False\n            \n            if not self._is_running:\n                self.progress_updated.emit(\"Scan cancelled.\", 0, 0)\n                self.finished.emit()\n                return\n\n            total_items = len(all_entries)\n            self.progress_updated.emit(f\"Scanning {total_items} items in {os.path.basename(target_path)}...\", 0, total_items)\n            \n            # --- Second Pass: Get sizes --- \n            for entry in all_entries:\n                if not self._is_running: break\n                items_processed += 1\n                self.progress_updated.emit(f\"Processing: {entry.name}\", items_processed, total_items)\n                item_path = entry.path\n                item_size = 0\n                try:\n                    if entry.is_dir(follow_symlinks=False):\n                        item_size = self._get_dir_size(item_path)\n                    elif entry.is_file(follow_symlinks=False):\n                        item_size = entry.stat(follow_symlinks=False).st_size\n                    else:\n                        # Skip symlinks, special files etc. for simplicity\n                        continue \n                        \n                    if item_size > 0: # Only include items with size > 0 for treemap clarity\n                        scan_data['items'].append({\n                            'name': entry.name,\n                            'size': item_size,\n                            'path': item_path\n                        })\n                        total_size += item_size\n                \n                except PermissionError:\n                    self.progress_updated.emit(f\"Skipping (permission denied): {entry.name}\", items_processed, total_items)\n                except FileNotFoundError: # Symlink might be broken\n                     self.progress_updated.emit(f\"Skipping (not found/broken link?): {entry.name}\", items_processed, total_items)\n                except Exception as e:\n                    self.progress_updated.emit(f\"Skipping (error): {entry.name} - {e}\", items_processed, total_items)\n\n            if self._is_running:\n                # Add 'Other files' if the top-level dir contains files directly\n                # This part is simplified; a proper scan might handle this differently.\n                self.progress_updated.emit(f\"Scan finished for {os.path.basename(target_path)}. Preparing results...\", total_items, total_items)\n                self.scan_complete.emit(scan_data)\n\n        except Exception as e:\n            if self._is_running:\n                self.error_occurred.emit(f\"An unexpected error occurred during scan: {e}\")\n        finally:\n            if self._is_running:\n                self._is_running = False\n            self.finished.emit()\n\n    def _get_dir_size(self, dir_path):\n        \"\"\"Recursively calculates the total size of a directory.\"\"\"\n        total_size = 0\n        if not self._is_running: return 0\n        try:\n            for dirpath, dirnames, filenames in os.walk(dir_path, topdown=True, onerror=self._handle_walk_error):\n                if not self._is_running: break\n                # Process files in the current directory\n                for f in filenames:\n                    if not self._is_running: break\n                    fp = os.path.join(dirpath, f)\n                    # skip if it is symbolic link or not a file\n                    if not os.path.islink(fp) and os.path.isfile(fp):\n                        try:\n                             total_size += os.path.getsize(fp)\n                        except FileNotFoundError:\n                             self.progress_updated.emit(f\"Skipping (link/not found): {fp}\", -1, -1) # Progress indeterminate here\n                        except PermissionError:\n                            self.progress_updated.emit(f\"Skipping (permission): {fp}\", -1, -1)\n                        except Exception as e:\n                             self.progress_updated.emit(f\"Skipping (error getting size): {fp} - {e}\", -1, -1)\n                if not self._is_running: break\n                # Remove dirs we can't access from further processing (already handled by onerror)\n                # dirnames[:] = [d for d in dirnames if os.access(os.path.join(dirpath, d), os.R_OK | os.X_OK)]\n\n        except PermissionError:\n             self.progress_updated.emit(f\"Skipping directory (permission): {dir_path}\", -1, -1)\n        except Exception as e:\n             self.progress_updated.emit(f\"Error walking directory {dir_path}: {e}\", -1, -1)\n        return total_size\n\n    def _handle_walk_error(self, os_error):\n        \"\"\"Handles errors during os.walk, typically permission errors.\"\"\"\n        if self._is_running:\n            self.progress_updated.emit(f\"Cannot access: {os_error.filename} ({os_error.strerror})\", -1, -1)\n        # We simply report the error and continue the walk where possible\n"
  },
  {
    "filename": "treemap_widget.py",
    "content": "import matplotlib\nmatplotlib.use('Qt5Agg') # Use the Qt5 backend\nimport matplotlib.pyplot as plt\nfrom matplotlib.backends.backend_qt5agg import FigureCanvasQTAgg as FigureCanvas\nimport squarify\nimport math\n\nfrom PyQt5.QtWidgets import QWidget, QVBoxLayout, QLabel\nfrom PyQt5.QtCore import Qt\n\n# Helper function for formatting sizes\ndef format_size(size_bytes):\n    if size_bytes == 0:\n       return \"0 B\"\n    size_name = (\"B\", \"KB\", \"MB\", \"GB\", \"TB\", \"PB\", \"EB\", \"ZB\", \"YB\")\n    i = int(math.floor(math.log(size_bytes, 1024)))\n    p = math.pow(1024, i)\n    s = round(size_bytes / p, 2)\n    return f\"{s} {size_name[i]}\"\n\nclass TreemapWidget(QWidget):\n    \"\"\"A widget to display disk usage using a Squarify treemap.\"\"\"\n    def __init__(self, parent=None):\n        super().__init__(parent)\n\n        self.figure, self.ax = plt.subplots() # Use subplots for better control\n        self.canvas = FigureCanvas(self.figure)\n        self.canvas.setToolTip(\"Disk Usage Treemap\") # Basic tooltip for the canvas\n\n        self._current_data = None\n        self._norm = None # For color mapping\n        self._cmap = plt.get_cmap('viridis') # Choose a colormap\n\n        layout = QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(self.canvas)\n\n        self._tooltip_label = QLabel(self.canvas) # Create tooltip label attached to canvas\n        self._tooltip_label.setVisible(False)\n        self._tooltip_label.setStyleSheet(\"\"\"\n            background-color: #FFFFE0; /* Light yellow */\n            color: black;\n            border: 1px solid black;\n            padding: 3px;\n            font-size: 9pt;\n        \"\"\")\n        self._tooltip_label.setWordWrap(True)\n        self._tooltip_label.setAlignment(Qt.AlignLeft | Qt.AlignTop)\n        self._tooltip_label.adjustSize()\n\n        # Connect mouse motion event for tooltips\n        self.canvas.mpl_connect('motion_notify_event', self.on_motion)\n        self.canvas.mpl_connect('axes_leave_event', self.on_leave)\n        \n        self.clear_plot() # Initial clear state\n\n    def clear_plot(self):\n        self.ax.clear()\n        self.ax.set_xticks([])\n        self.ax.set_yticks([])\n        self.ax.set_title(\"Select a directory and click 'Analyze'\")\n        self.canvas.draw()\n        self._current_data = None # Reset data\n\n    def update_plot(self, data):\n        \"\"\"Updates the treemap with new data.\"\"\"\n        self.ax.clear()\n        self._current_data = data # Store data for tooltips\n        items = data.get('items', []) # Expecting list of dicts\n        target_path = data.get('path', 'Unknown Path')\n\n        if not items:\n            self.ax.set_title(f\"No sizable items found in: {os.path.basename(target_path)}\")\n            self.ax.set_xticks([])\n            self.ax.set_yticks([])\n            self.canvas.draw()\n            return\n\n        sizes = [item['size'] for item in items]\n        # Prepare labels with names and formatted sizes\n        labels = [\n            f\"{item['name']}\\n({format_size(item['size'])})\"\n            for item in items\n        ]\n        total_size = sum(sizes)\n\n        # Normalize sizes for color mapping (optional, adds visual variance)\n        self._norm = matplotlib.colors.Normalize(vmin=min(sizes), vmax=max(sizes))\n        colors = [self._cmap(self._norm(value)) for value in sizes]\n        \n        try:\n            # Generate the treemap plot using squarify\n            self._rects = squarify.plot(\n                sizes=sizes,\n                label=labels,\n                color=colors,\n                alpha=0.8,\n                ax=self.ax,\n                text_kwargs={'fontsize': 8, 'wrap': True}, # Adjust font size\n                bar_kwargs={'linewidth':1, 'edgecolor':'#FFFFFF'} # White borders\n            )\n            \n            self.ax.set_title(f\"Disk Usage Treemap for: {target_path}\\nTotal: {format_size(total_size)}\")\n            self.ax.set_xticks([])\n            self.ax.set_yticks([])\n            plt.tight_layout() # Adjust layout\n            self.canvas.draw()\n        except Exception as e:\n             self.ax.set_title(f\"Error plotting treemap: {e}\")\n             print(f\"Error plotting treemap: {e}\") # Log error\n             self.canvas.draw()\n\n    def on_motion(self, event):\n        \"\"\"Handle mouse motion over the canvas to show tooltips.\"\"\"\n        if event.inaxes != self.ax or not self._current_data or not self._rects:\n            self.hide_tooltip()\n            return\n\n        cont, ind = self.ax.contains(event)\n        if cont:\n             # Find which rectangle contains the event\n             # This requires checking the event coordinates against rectangle bounds\n            found_item = None\n            for i, rect in enumerate(self.ax.patches): # Iterate through the patches (rectangles)\n                bbox = rect.get_bbox()\n                if bbox.contains(event.x, event.y):\n                    if i < len(self._current_data['items']):\n                         found_item = self._current_data['items'][i]\n                    break\n            \n            if found_item:\n                tooltip_text = f\"{found_item['name']}\\nPath: {found_item['path']}\\nSize: {format_size(found_item['size'])}\"\n                self.show_tooltip(event.x, event.y, tooltip_text)\n            else:\n                self.hide_tooltip()\n        else:\n            self.hide_tooltip()\n            \n    def on_leave(self, event):\n        \"\"\"Hide tooltip when mouse leaves the axes area.\"\"\"\n        self.hide_tooltip()\n\n    def show_tooltip(self, x, y, text):\n        \"\"\"Displays the tooltip at the given canvas coordinates.\"\"\"\n        # Convert matplotlib display coords to Qt widget coords\n        point = self.canvas.mapFromGlobal(self.canvas.cursor().pos())\n        \n        self._tooltip_label.setText(text)\n        self._tooltip_label.adjustSize() # Adjust size based on text\n        # Position tooltip slightly offset from cursor\n        # Clamp position to stay within canvas bounds\n        tx = max(0, min(point.x() + 15, self.canvas.width() - self._tooltip_label.width()))\n        ty = max(0, min(point.y() + 10, self.canvas.height() - self._tooltip_label.height()))\n        self._tooltip_label.move(tx, ty)\n        self._tooltip_label.setVisible(True)\n        self._tooltip_label.raise_() # Bring to front\n\n    def hide_tooltip(self):\n        self._tooltip_label.setVisible(False)\n"
  },
  {
    "filename": "treemap_gui.py",
    "content": "import sys\nimport os\nfrom PyQt5.QtWidgets import (\n    QApplication, QMainWindow, QWidget, QVBoxLayout, QHBoxLayout,\n    QPushButton, QLabel, QLineEdit, QFileDialog, QStatusBar,\n    QMessageBox, QProgressBar\n)\nfrom PyQt5.QtCore import Qt, QThread, pyqtSignal, QObject\n\nfrom disk_scan_logic import DiskScanLogic\nfrom treemap_widget import TreemapWidget # Import the custom widget\n\nclass Worker(QObject):\n    \"\"\"Worker thread to run the disk scan logic.\"\"\"\n    progress_updated = pyqtSignal(str, int, int)\n    scan_complete = pyqtSignal(dict)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, logic_instance):\n        super().__init__()\n        self.logic = logic_instance\n        self._target_path = None\n\n    def run(self):\n        # Connect logic signals to worker signals\n        self.logic.progress_updated.connect(self.progress_updated)\n        self.logic.scan_complete.connect(self.scan_complete)\n        self.logic.error_occurred.connect(self.error_occurred)\n        self.logic.finished.connect(self.on_logic_finished)\n\n        if self._target_path:\n            self.logic.start_scan(self._target_path)\n        else:\n             self.error_occurred.emit(\"Worker started without a target path.\")\n             self.on_logic_finished()\n\n    def set_target_path(self, path):\n        self._target_path = path\n\n    def stop(self):\n        self.logic.stop()\n\n    def on_logic_finished(self):\n        # Disconnect signals\n        try:\n            self.logic.progress_updated.disconnect(self.progress_updated)\n            self.logic.scan_complete.disconnect(self.scan_complete)\n            self.logic.error_occurred.disconnect(self.error_occurred)\n            self.logic.finished.disconnect(self.on_logic_finished)\n        except TypeError:\n             pass # Ignore if already disconnected\n        self.finished.emit()\n\n\nclass TreemapApp(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle(\"Disk Usage Treemap Analyzer\")\n        self.setGeometry(100, 100, 900, 700)\n\n        self.central_widget = QWidget()\n        self.setCentralWidget(self.central_widget)\n        self.layout = QVBoxLayout(self.central_widget)\n\n        # --- Worker Thread Setup ---\n        self.thread = None\n        self.worker = None\n        self.logic = DiskScanLogic()\n\n        # --- Directory Selection ---\n        select_layout = QHBoxLayout()\n        self.dir_label = QLabel(\"Directory:\")\n        self.dir_edit = QLineEdit()\n        self.dir_edit.setPlaceholderText(\"Select directory to analyze...\")\n        self.browse_button = QPushButton(\"Browse...\")\n        self.browse_button.clicked.connect(self.browse_directory)\n        select_layout.addWidget(self.dir_label)\n        select_layout.addWidget(self.dir_edit)\n        select_layout.addWidget(self.browse_button)\n        self.layout.addLayout(select_layout)\n\n        # --- Controls ---\n        control_layout = QHBoxLayout()\n        self.analyze_button = QPushButton(\"Analyze\")\n        self.analyze_button.clicked.connect(self.start_analysis_task)\n        self.stop_button = QPushButton(\"Stop Scan\")\n        self.stop_button.clicked.connect(self.stop_task)\n        self.stop_button.setEnabled(False)\n        control_layout.addWidget(self.analyze_button)\n        control_layout.addWidget(self.stop_button)\n        control_layout.addStretch()\n        self.layout.addLayout(control_layout)\n\n        # --- Treemap Display Area ---\n        self.treemap_widget = TreemapWidget(self)\n        self.layout.addWidget(self.treemap_widget, stretch=1) # Give it more space\n\n        # --- Progress Bar ---\n        self.progress_bar = QProgressBar()\n        self.progress_bar.setTextVisible(False)\n        self.progress_bar.setVisible(False) # Hide initially\n        self.layout.addWidget(self.progress_bar)\n\n        # --- Status Bar ---\n        self.status_bar = QStatusBar()\n        self.setStatusBar(self.status_bar)\n        self.status_bar.showMessage(\"Ready. Select a directory to analyze.\")\n\n    def browse_directory(self):\n        dir_path = QFileDialog.getExistingDirectory(self, \"Select Directory to Analyze\")\n        if dir_path:\n            self.dir_edit.setText(dir_path)\n            self.status_bar.showMessage(f\"Selected: {dir_path}. Click 'Analyze'.\")\n            self.treemap_widget.clear_plot() # Clear previous plot\n\n    def toggle_controls(self, enable):\n        \"\"\"Enable/disable controls during analysis.\"\"\"\n        self.browse_button.setEnabled(enable)\n        self.analyze_button.setEnabled(enable)\n        self.dir_edit.setEnabled(enable)\n        self.stop_button.setEnabled(not enable)\n        self.progress_bar.setVisible(not enable)\n\n    def start_analysis_task(self):\n        target_path = self.dir_edit.text()\n        if not target_path or not os.path.isdir(target_path):\n            QMessageBox.warning(self, \"Invalid Directory\",\n                                \"Please select a valid directory before analyzing.\")\n            return\n\n        if self.thread and self.thread.isRunning():\n            QMessageBox.warning(self, \"Busy\", \"An analysis is already in progress.\")\n            return\n\n        self.status_bar.showMessage(f\"Starting analysis for: {target_path}\")\n        self.treemap_widget.clear_plot() # Clear plot before starting\n        self.treemap_widget.ax.set_title(\"Scanning disk...\")\n        self.treemap_widget.canvas.draw() # Update title\n        self.toggle_controls(False)\n        self.progress_bar.setRange(0, 0) # Indeterminate initially\n        self.progress_bar.setValue(0)\n\n        self.thread = QThread()\n        self.worker = Worker(self.logic)\n        self.worker.moveToThread(self.thread)\n\n        # Connect worker signals to GUI slots\n        self.worker.progress_updated.connect(self.update_progress)\n        self.worker.scan_complete.connect(self.display_treemap)\n        self.worker.error_occurred.connect(self.show_error)\n        self.worker.finished.connect(self.on_task_finished)\n\n        self.worker.set_target_path(target_path)\n        self.thread.started.connect(self.worker.run)\n        self.thread.start()\n\n    def stop_task(self):\n        if self.worker:\n            self.status_bar.showMessage(\"Stopping analysis...\")\n            self.worker.stop()\n            self.stop_button.setEnabled(False) # Prevent multiple clicks\n\n    def update_progress(self, message, current, total):\n        self.status_bar.showMessage(message)\n        if total > 0:\n             if self.progress_bar.maximum() != total:\n                  self.progress_bar.setRange(0, total)\n             self.progress_bar.setValue(current)\n             self.progress_bar.setFormat(f\"%v / %m ({int(current * 100 / total)}%)\")\n             self.progress_bar.setTextVisible(True)\n        elif current == -1 and total == -1: # Special case for indeterminate sub-ops\n             pass # Keep progress bar as is, message is in status bar\n        else: # total is 0, indeterminate\n             self.progress_bar.setRange(0, 0)\n             self.progress_bar.setTextVisible(False)\n\n    def display_treemap(self, data):\n        self.status_bar.showMessage(\"Scan complete. Displaying treemap...\")\n        self.treemap_widget.update_plot(data)\n        self.status_bar.showMessage(\"Analysis complete.\", 5000)\n\n    def show_error(self, message):\n        QMessageBox.critical(self, \"Error\", message)\n        self.status_bar.showMessage(f\"Error: {message}\")\n        # Optionally clear plot or show error state on plot\n        self.treemap_widget.clear_plot()\n        self.treemap_widget.ax.set_title(\"Analysis failed. See status bar/log.\")\n        self.treemap_widget.canvas.draw()\n\n    def on_task_finished(self):\n        if self.thread is None: return # Safety\n\n        self.thread.quit()\n        self.thread.wait()\n\n        final_message = self.status_bar.currentMessage()\n\n        self.thread = None\n        self.worker = None\n\n        self.toggle_controls(True)\n        self.progress_bar.setVisible(False)\n        self.stop_button.setEnabled(False) # Ensure stop button is off\n\n        # Keep final status message if it indicates completion state or error\n        if \"complete\" not in final_message.lower() and \\
           \"error\" not in final_message.lower() and \\
           \"fail\" not in final_message.lower() and \\
           \"stopping\" not in final_message.lower():\n            self.status_bar.showMessage(\"Ready\", 3000)\n\n    def closeEvent(self, event):\n        # Ensure thread is stopped cleanly on exit\n        if self.thread is not None and self.thread.isRunning():\n            self.status_bar.showMessage(\"Stopping background scan before closing...\")\n            if self.worker:\n                 self.worker.stop()\n            self.thread.quit()\n            self.thread.wait(2000) # Wait max 2 seconds\n        event.accept()\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    main_win = TreemapApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "main.py",
    "content": "import sys\nfrom PyQt5.QtWidgets import QApplication\nfrom PyQt5.QtCore import Qt\nfrom treemap_gui import TreemapApp\n\nif __name__ == '__main__':\n    # Enable high DPI scaling for better visuals on some systems\n    if hasattr(QApplication, 'setAttribute'):\n        QApplication.setAttribute(Qt.AA_EnableHighDpiScaling, True)\n        QApplication.setAttribute(Qt.AA_UseHighDpiPixmaps, True)\n\n    app = QApplication(sys.argv)\n    main_win = TreemapApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "README.md",
    "content": "# Disk Usage Treemap Analyzer\n\nA Python tool with a PyQt5 GUI that analyzes the disk usage within a selected directory and displays it as an interactive treemap.\n\n## Features\n\n*   **Treemap Visualization:** Uses `matplotlib` and `squarify` to generate a treemap where the area of each rectangle is proportional to the disk space used by a file or subdirectory within the selected path.\n*   **Directory Scanning:** Recursively scans the selected directory to calculate the size of top-level items (files and subdirectories).\n*   **Interactive Tooltips:** Hovering over a rectangle in the treemap displays the name, full path, and formatted size of the corresponding item.\n*   **User Interface:** Simple GUI built with PyQt5.\n*   **Responsiveness:** Uses a separate thread for disk scanning to keep the GUI responsive.\n*   **Progress Indication:** Shows progress during the scan via a progress bar and status messages.\n*   **Cancellation:** Allows stopping a running scan.\n*   **Error Handling:** Reports permission errors and other issues encountered during scanning.\n\n## Requirements\n\n*   Python 3.x\n*   PyQt5 (`pip install PyQt5`)\n*   Matplotlib (`pip install matplotlib`)\n*   Squarify (`pip install squarify`)\n\n## How to Run\n\n1.  Save the following files in the same directory:\n    *   `main.py`\n    *   `treemap_gui.py`\n    *   `treemap_widget.py`\n    *   `disk_scan_logic.py`\n    *   `README.md` (this file)\n2.  Install the required libraries:\n    ```bash\n    pip install PyQt5 matplotlib squarify\n    ```\n3.  Run the main script:\n    ```bash\n    python main.py\n    ```\n\n## How to Use\n\n1.  **Select Directory:** Click the **Browse...** button and choose the directory you want to analyze.\n2.  **Start Analysis:** Click the **Analyze** button. The application will begin scanning the directory.\n3.  **Monitor Progress:** The progress bar and status bar will show the progress of the scan. Scanning large directories or directories with many files can take time.\n4.  **View Treemap:** Once the scan is complete, the treemap will be displayed. Each colored rectangle represents a file or subdirectory within the initially selected directory.\n    *   The **size** of each rectangle is proportional to the disk space used by that item.\n    *   The **label** shows the item's name and formatted size.\n    *   **Hover** over a rectangle with your mouse to see a tooltip with the full path and size.\n5.  **Stop Scan:** If a scan is taking too long or you selected the wrong directory, click the **Stop Scan** button.\n\n**Note:** The scan calculates the total size of subdirectories by walking through them. This can be I/O intensive. Items with zero size are typically excluded from the treemap for clarity.\n"
  }
]
