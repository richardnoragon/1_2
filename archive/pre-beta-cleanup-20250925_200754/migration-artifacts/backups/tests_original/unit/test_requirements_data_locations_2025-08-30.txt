# Test dependencies for data_locations.py unit tests
# Install with: pip install -r test_requirements_data_locations_2025-08-30.txt

# Core testing framework
pytest>=7.0.0
pytest-html>=3.1.0
pytest-json-report>=1.5.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Test utilities and assertions
pytest-xdist>=3.0.0  # For parallel test execution
pytest-timeout>=2.1.0  # For test timeouts
pytest-benchmark>=4.0.0  # For performance benchmarking

# Code coverage and quality
coverage>=7.0.0
pytest-clarity>=1.0.1  # Better assertion output

# Mocking and fixtures
responses>=0.22.0  # For HTTP mocking if needed
freezegun>=1.2.0  # For time mocking if needed

# Documentation and reporting
pytest-metadata>=2.0.0  # Additional metadata in reports
pytest-instafail>=0.4.2  # Immediate failure reporting

# Platform-specific dependencies
pathlib2>=2.3.7; python_version < "3.4"  # Backport for older Python

# Development and debugging
pytest-pdb>=0.2.0  # Enhanced debugging
pytest-sugar>=0.9.6  # Better test output formatting