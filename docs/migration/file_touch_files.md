[
  {
    "filename": "file_touch_logic.py",
    "content": "import os\nimport time\nimport platform\nfrom datetime import datetime, timezone\nfrom PyQt5.QtCore import QObject, pyqtSignal\n\n# Note: Reliably *setting* creation time is platform-specific and often requires\n# extra privileges or libraries (like pywin32 on Windows).\n# Standard os.utime only modifies access and modification times.\n\nclass FileTouchLogic(QObject):\n    \"\"\"Handles the logic for getting and setting file timestamps.\"\"\"\n    timestamps_fetched = pyqtSignal(dict)  # { 'access': datetime, 'modification': datetime, 'creation': datetime/None }\n    operation_result = pyqtSignal(bool, str) # success (bool), message (str)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self):\n        super().__init__()\n        self._is_running = False\n\n    def stop(self):\n        # Timestamp operations are usually very fast, stop might not be effective\n        # but include for consistency with worker pattern.\n        self._is_running = False\n\n    def get_file_timestamps(self, filepath):\n        \"\"\"Fetches access, modification, and creation timestamps for a file.\"\"\"\n        self._is_running = True\n        if not self._is_running: return\n\n        try:\n            if not os.path.exists(filepath):\n                raise FileNotFoundError(f\"File not found: {filepath}\")\n            if not os.path.isfile(filepath):\n                 raise ValueError(f\"Path is not a file: {filepath}\")\n\n            stat_result = os.stat(filepath)\n\n            # Access Time (atime)\n            atime_ts = stat_result.st_atime\n            atime_dt = datetime.fromtimestamp(atime_ts, tz=timezone.utc).astimezone()\n\n            # Modification Time (mtime)\n            mtime_ts = stat_result.st_mtime\n            mtime_dt = datetime.fromtimestamp(mtime_ts, tz=timezone.utc).astimezone()\n\n            # Creation Time (birthtime or ctime)\n            # Note: ctime on Unix is 'change time', not creation time.\n            # birthtime is available on some systems (macOS, BSD, some Linux)\n            ctime_dt = None\n            if hasattr(stat_result, 'st_birthtime') and stat_result.st_birthtime:\n                ctime_ts = stat_result.st_birthtime\n                ctime_dt = datetime.fromtimestamp(ctime_ts, tz=timezone.utc).astimezone()\n            elif platform.system() == \"Windows\":\n                # On Windows, ctime is creation time\n                 ctime_ts = stat_result.st_ctime\n                 ctime_dt = datetime.fromtimestamp(ctime_ts, tz=timezone.utc).astimezone()\n            # Else: ctime_dt remains None (or we could report Unix ctime if needed)\n\n            result = {\n                'access': atime_dt,\n                'modification': mtime_dt,\n                'creation': ctime_dt\n            }\n            self.timestamps_fetched.emit(result)\n            self.operation_result.emit(True, \"Timestamps fetched successfully.\")\n\n        except FileNotFoundError as e:\n             self.error_occurred.emit(str(e))\n             self.operation_result.emit(False, str(e))\n        except ValueError as e:\n             self.error_occurred.emit(str(e))\n             self.operation_result.emit(False, str(e))\n        except PermissionError:\n             err_msg = f\"Permission denied: {filepath}\"\n             self.error_occurred.emit(err_msg)\n             self.operation_result.emit(False, err_msg)\n        except Exception as e:\n            err_msg = f\"An unexpected error occurred: {e}\"\n            self.error_occurred.emit(err_msg)\n            self.operation_result.emit(False, err_msg)\n        finally:\n            self._is_running = False\n            self.finished.emit()\n\n\n    def set_file_timestamps(self, filepath, modify_access, modify_mod, set_to_current, specific_dt):\n        \"\"\"Sets the access and modification timestamps for a file.\"\"\"\n        self._is_running = True\n        if not self._is_running: return\n\n        try:\n            if not os.path.exists(filepath):\n                raise FileNotFoundError(f\"File not found: {filepath}\")\n            if not os.path.isfile(filepath):\n                 raise ValueError(f\"Path is not a file: {filepath}\")\n            if not modify_access and not modify_mod:\n                raise ValueError(\"No timestamp selected for modification.\")\n\n            # Determine the target timestamps in epoch seconds\n            current_stat = os.stat(filepath)\n            target_atime_ts = current_stat.st_atime\n            target_mtime_ts = current_stat.st_mtime\n\n            if set_to_current:\n                now_ts = time.time() # Use time.time() for os.utime compatibility\n                if modify_access:\n                    target_atime_ts = now_ts\n                if modify_mod:\n                    target_mtime_ts = now_ts\n            else:\n                if specific_dt is None:\n                    raise ValueError(\"Specific date/time not provided.\")\n                # Convert aware datetime from GUI (likely local) to UTC timestamp\n                target_ts = specific_dt.timestamp()\n                if modify_access:\n                    target_atime_ts = target_ts\n                if modify_mod:\n                    target_mtime_ts = target_ts\n\n            # Apply the timestamps using os.utime\n            # os.utime takes (path, (atime, mtime)) where times are epoch seconds\n            os.utime(filepath, (target_atime_ts, target_mtime_ts))\n            self.operation_result.emit(True, f\"Timestamps updated successfully for {os.path.basename(filepath)}.\")\n\n        except FileNotFoundError as e:\n             self.error_occurred.emit(str(e))\n             self.operation_result.emit(False, str(e))\n        except ValueError as e:\n             self.error_occurred.emit(str(e))\n             self.operation_result.emit(False, str(e))\n        except PermissionError:\n             err_msg = f\"Permission denied: Cannot modify timestamps for {filepath}\"\n             self.error_occurred.emit(err_msg)\n             self.operation_result.emit(False, err_msg)\n        except Exception as e:\n            err_msg = f\"An unexpected error occurred during update: {e}\"\n            self.error_occurred.emit(err_msg)\n            self.operation_result.emit(False, err_msg)\n        finally:\n            self._is_running = False\n            self.finished.emit()\n"
  },
  {
    "filename": "file_touch_gui.py",
    "content": "import sys\nimport os\nfrom datetime import datetime\nfrom PyQt5.QtWidgets import (\n    QApplication, QMainWindow, QWidget, QVBoxLayout, QHBoxLayout,\n    QPushButton, QLabel, QLineEdit, QFileDialog, QStatusBar,\n    QMessageBox, QCheckBox, QRadioButton, QDateTimeEdit, QGroupBox,\n    QSizePolicy\n)\nfrom PyQt5.QtCore import Qt, QThread, pyqtSignal, QObject, QDateTime\n\nfrom file_touch_logic import FileTouchLogic\n\nclass Worker(QObject):\n    \"\"\"Worker to run logic methods in a separate thread.\"\"\"\n    timestamps_fetched = pyqtSignal(dict)\n    operation_result = pyqtSignal(bool, str)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, logic_instance):\n        super().__init__()\n        self.logic = logic_instance\n        self._mode = None # 'get' or 'set'\n        self._args = None\n\n    def run(self):\n        # Connect logic signals to worker signals\n        self.logic.timestamps_fetched.connect(self.timestamps_fetched)\n        self.logic.operation_result.connect(self.operation_result)\n        self.logic.error_occurred.connect(self.error_occurred)\n        self.logic.finished.connect(self.on_logic_finished)\n\n        try:\n            if self._mode == 'get' and self._args:\n                self.logic.get_file_timestamps(self._args[0])\n            elif self._mode == 'set' and self._args:\n                self.logic.set_file_timestamps(*self._args)\n            else:\n                self.error_occurred.emit(\"Worker started with invalid mode or arguments.\")\n                self.on_logic_finished() # Trigger finish\n        except Exception as e:\n            self.error_occurred.emit(f\"Critical error in worker execution: {e}\")\n            self.on_logic_finished() # Trigger finish\n\n    def start_get(self, filepath):\n        self._mode = 'get'\n        self._args = [filepath]\n\n    def start_set(self, filepath, mod_a, mod_m, current, dt):\n        self._mode = 'set'\n        self._args = [filepath, mod_a, mod_m, current, dt]\n\n    def stop(self):\n        self.logic.stop()\n\n    def on_logic_finished(self):\n        # Disconnect signals to prevent duplicates if reused\n        try:\n            self.logic.timestamps_fetched.disconnect(self.timestamps_fetched)\n            self.logic.operation_result.disconnect(self.operation_result)\n            self.logic.error_occurred.disconnect(self.error_occurred)\n            self.logic.finished.disconnect(self.on_logic_finished)\n        except TypeError:\n            pass # Ignore if signals were already disconnected\n        self.finished.emit()\n\n\nclass FileTouchApp(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle(\"File Timestamp Tool (Touch)\")\n        self.setGeometry(100, 100, 550, 450)\n\n        self.central_widget = QWidget()\n        self.setCentralWidget(self.central_widget)\n        self.layout = QVBoxLayout(self.central_widget)\n\n        # --- Worker Thread Setup ---\n        self.thread = None\n        self.worker = None\n        self.logic = FileTouchLogic()\n\n        # --- File Selection ---\n        select_layout = QHBoxLayout()\n        self.file_label = QLabel(\"File:\")\n        self.file_edit = QLineEdit()\n        self.file_edit.setPlaceholderText(\"Select a file to modify its timestamps\")\n        self.file_edit.textChanged.connect(self.on_file_path_changed)\n        self.browse_button = QPushButton(\"Browse...\")\n        self.browse_button.clicked.connect(self.browse_file)\n        select_layout.addWidget(self.file_label)\n        select_layout.addWidget(self.file_edit)\n        select_layout.addWidget(self.browse_button)\n        self.layout.addLayout(select_layout)\n\n        # --- Current Timestamps Display ---\n        current_ts_group = QGroupBox(\"Current Timestamps\")\n        current_ts_layout = QVBoxLayout()\n        self.current_access_label = QLabel(\"Access Time:       -\")\n        self.current_mod_label = QLabel(\"Modification Time: -\")\n        self.current_creation_label = QLabel(\"Creation Time:     -\")\n        self.get_current_button = QPushButton(\"Get Current Timestamps\")\n        self.get_current_button.clicked.connect(self.start_get_timestamps_task)\n        self.get_current_button.setEnabled(False)\n        current_ts_layout.addWidget(self.current_access_label)\n        current_ts_layout.addWidget(self.current_mod_label)\n        current_ts_layout.addWidget(self.current_creation_label)\n        current_ts_layout.addWidget(self.get_current_button)\n        current_ts_group.setLayout(current_ts_layout)\n        self.layout.addWidget(current_ts_group)\n\n        # --- Timestamp Modification Options ---\n        mod_group = QGroupBox(\"Modify Timestamps\")\n        mod_layout = QVBoxLayout()\n\n        # Which timestamps to modify\n        self.cb_access = QCheckBox(\"Modify Access Time (atime)\")\n        self.cb_mod = QCheckBox(\"Modify Modification Time (mtime)\")\n        self.cb_access.toggled.connect(self.check_apply_button_state)\n        self.cb_mod.toggled.connect(self.check_apply_button_state)\n        mod_layout.addWidget(self.cb_access)\n        mod_layout.addWidget(self.cb_mod)\n\n        # How to set the time\n        time_options_group = QGroupBox(\"Set To\")\n        time_options_layout = QVBoxLayout()\n        self.rb_current_time = QRadioButton(\"Current System Time\")\n        self.rb_specific_time = QRadioButton(\"Specific Date and Time\")\n        self.datetime_edit = QDateTimeEdit()\n        self.datetime_edit.setCalendarPopup(True)\n        self.datetime_edit.setDateTime(QDateTime.currentDateTime())\n        self.datetime_edit.setEnabled(False) # Disabled by default\n        self.rb_current_time.setChecked(True)\n        self.rb_current_time.toggled.connect(self.toggle_datetime_edit)\n\n        time_options_layout.addWidget(self.rb_current_time)\n        time_options_layout.addWidget(self.rb_specific_time)\n        time_options_layout.addWidget(self.datetime_edit)\n        time_options_group.setLayout(time_options_layout)\n        mod_layout.addWidget(time_options_group)\n\n        mod_group.setLayout(mod_layout)\n        self.layout.addWidget(mod_group)\n\n        # --- Apply Button ---\n        self.apply_button = QPushButton(\"Apply Timestamps\")\n        self.apply_button.clicked.connect(self.start_set_timestamps_task)\n        self.apply_button.setEnabled(False)\n        self.layout.addWidget(self.apply_button)\n\n        self.layout.addStretch() # Push status bar to bottom\n\n        # --- Status Bar ---\n        self.status_bar = QStatusBar()\n        self.setStatusBar(self.status_bar)\n        self.status_bar.showMessage(\"Ready. Select a file.\")\n\n    def browse_file(self):\n        filepath, _ = QFileDialog.getOpenFileName(self, \"Select File\")\n        if filepath:\n            self.file_edit.setText(filepath)\n            # Optionally trigger 'get timestamps' automatically\n            # self.start_get_timestamps_task()\n\n    def on_file_path_changed(self, filepath):\n        is_valid_file = os.path.isfile(filepath)\n        self.get_current_button.setEnabled(is_valid_file)\n        self.check_apply_button_state()\n        # Clear current timestamps when file changes\n        self.current_access_label.setText(\"Access Time:       -\")\n        self.current_mod_label.setText(\"Modification Time: -\")\n        self.current_creation_label.setText(\"Creation Time:     -\")\n        if not is_valid_file and filepath: # If path entered but not valid file\n            self.status_bar.showMessage(\"Selected path is not a valid file.\")\n        elif not filepath:\n             self.status_bar.showMessage(\"Ready. Select a file.\")\n        else:\n             self.status_bar.showMessage(f\"File selected: {os.path.basename(filepath)}. Get timestamps or set new ones.\")\n\n    def toggle_datetime_edit(self):\n        self.datetime_edit.setEnabled(self.rb_specific_time.isChecked())\n\n    def check_apply_button_state(self):\n        filepath = self.file_edit.text()\n        is_valid_file = os.path.isfile(filepath)\n        is_any_ts_selected = self.cb_access.isChecked() or self.cb_mod.isChecked()\n        self.apply_button.setEnabled(is_valid_file and is_any_ts_selected)\n\n    def toggle_controls(self, enable):\n        \"\"\"Enable/disable controls during operation.\"\"\"\n        self.browse_button.setEnabled(enable)\n        self.file_edit.setEnabled(enable)\n        self.get_current_button.setEnabled(enable and os.path.isfile(self.file_edit.text()))\n        self.cb_access.setEnabled(enable)\n        self.cb_mod.setEnabled(enable)\n        self.rb_current_time.setEnabled(enable)\n        self.rb_specific_time.setEnabled(enable)\n        # Only enable datetime_edit if specific time is selected AND controls are enabled\n        self.datetime_edit.setEnabled(enable and self.rb_specific_time.isChecked())\n        # Apply button state depends on other factors, managed by check_apply_button_state\n        if enable:\n             self.check_apply_button_state()\n        else:\n             self.apply_button.setEnabled(False)\n\n    def start_get_timestamps_task(self):\n        filepath = self.file_edit.text()\n        if not filepath or not os.path.isfile(filepath):\n            QMessageBox.warning(self, \"Invalid File\", \"Please select a valid file first.\")\n            return\n\n        if self.thread and self.thread.isRunning():\n            QMessageBox.warning(self, \"Busy\", \"An operation is already in progress.\")\n            return\n\n        self.status_bar.showMessage(f\"Getting timestamps for {os.path.basename(filepath)}...\")\n        self.toggle_controls(False)\n\n        self.thread = QThread()\n        self.worker = Worker(self.logic)\n        self.worker.moveToThread(self.thread)\n\n        # Connect signals\n        self.worker.timestamps_fetched.connect(self.display_timestamps)\n        self.worker.operation_result.connect(self.handle_operation_result)\n        self.worker.error_occurred.connect(self.show_error)\n        self.worker.finished.connect(self.on_task_finished)\n\n        self.thread.started.connect(lambda: self.worker.start_get(filepath))\n        self.thread.start()\n\n    def start_set_timestamps_task(self):\n        filepath = self.file_edit.text()\n        if not filepath or not os.path.isfile(filepath):\n            QMessageBox.warning(self, \"Invalid File\", \"Please select a valid file first.\")\n            return\n\n        mod_access = self.cb_access.isChecked()\n        mod_mod = self.cb_mod.isChecked()\n        set_to_current = self.rb_current_time.isChecked()\n        specific_dt_qdt = self.datetime_edit.dateTime() # QDateTime\n        # Convert QDateTime to Python datetime (aware of local timezone)\n        specific_dt_py = specific_dt_qdt.toPyDateTime()\n\n        if not mod_access and not mod_mod:\n            QMessageBox.warning(self, \"No Selection\", \"Please select at least one timestamp (access/modification) to modify.\")\n            return\n\n        if self.thread and self.thread.isRunning():\n            QMessageBox.warning(self, \"Busy\", \"An operation is already in progress.\")\n            return\n\n        self.status_bar.showMessage(f\"Applying timestamps to {os.path.basename(filepath)}...\")\n        self.toggle_controls(False)\n\n        self.thread = QThread()\n        self.worker = Worker(self.logic)\n        self.worker.moveToThread(self.thread)\n\n        # Connect signals\n        self.worker.operation_result.connect(self.handle_operation_result)\n        self.worker.error_occurred.connect(self.show_error)\n        self.worker.finished.connect(self.on_task_finished)\n\n        self.thread.started.connect(lambda: self.worker.start_set(filepath, mod_access, mod_mod, set_to_current, specific_dt_py))\n        self.thread.start()\n\n    def display_timestamps(self, ts_dict):\n        dt_format = \"%Y-%m-%d %H:%M:%S %Z%z\"\n        access_str = ts_dict['access'].strftime(dt_format) if ts_dict.get('access') else 'N/A'\n        mod_str = ts_dict['modification'].strftime(dt_format) if ts_dict.get('modification') else 'N/A'\n        creation_str = ts_dict['creation'].strftime(dt_format) if ts_dict.get('creation') else 'N/A (or not available)'\n\n        self.current_access_label.setText(f\"Access Time:       {access_str}\")\n        self.current_mod_label.setText(f\"Modification Time: {mod_str}\")\n        self.current_creation_label.setText(f\"Creation Time:     {creation_str}\")\n\n    def handle_operation_result(self, success, message):\n        self.status_bar.showMessage(message)\n        if success:\n            # If setting was successful, refresh the displayed timestamps\n            if self.worker and self.worker._mode == 'set':\n                 self.start_get_timestamps_task() # Chain get operation after set\n                 # Note: The GUI will be disabled during this get operation as well.\n                 # Control re-enabling happens when the *final* task finishes.\n                 return # Don't re-enable controls yet\n        else:\n             QMessageBox.warning(self, \"Operation Failed\", message)\n        # If it wasn't a successful 'set' or it was a 'get', re-enable controls here\n        # (This path is hit after a successful 'get' or any failed operation)\n        # self.toggle_controls(True) # Re-enabling is now handled in on_task_finished\n\n    def show_error(self, message):\n        # This usually duplicates info from operation_result(False,...)\n        # but can catch worker-level errors.\n        # QMessageBox.critical(self, \"Error\", message)\n        self.status_bar.showMessage(f\"Error: {message}\")\n\n    def on_task_finished(self):\n        if self.thread is None: return # Safety check\n\n        self.thread.quit()\n        self.thread.wait()\n\n        # Only re-enable controls once *all* chained operations are done\n        # Check if another task was chained (like get after set)\n        # If the thread is already gone, means the chained task finished or no chaining occurred.\n        if not (self.thread and self.thread.isRunning()):\n             self.toggle_controls(True)\n             # Update status bar if it wasn't showing a specific result/error\n             current_msg = self.status_bar.currentMessage()\n             if \"successfully\" not in current_msg.lower() and \"failed\" not in current_msg.lower() and \"error\" not in current_msg.lower():\n                  self.status_bar.showMessage(\"Ready\", 3000)\n\n        self.thread = None\n        self.worker = None\n\n    def closeEvent(self, event):\n        # Ensure thread is stopped cleanly on exit\n        if self.thread is not None and self.thread.isRunning():\n            # Timestamp operations are fast, stopping might not be necessary\n            # but good practice if logic could potentially hang\n            # self.worker.stop()\n            self.thread.quit()\n            self.thread.wait(500) # Wait briefly\n        event.accept()\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    main_win = FileTouchApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "main.py",
    "content": "import sys\nfrom PyQt5.QtWidgets import QApplication\nfrom PyQt5.QtCore import Qt\nfrom file_touch_gui import FileTouchApp\n\nif __name__ == '__main__':\n    # Enable high DPI scaling for better visuals on some systems\n    if hasattr(QApplication, 'setAttribute'):\n        QApplication.setAttribute(Qt.AA_EnableHighDpiScaling, True)\n        QApplication.setAttribute(Qt.AA_UseHighDpiPixmaps, True)\n\n    app = QApplication(sys.argv)\n    main_win = FileTouchApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "README.md",
    "content": "# File Timestamp Tool (Touch)\n\nA Python tool with a PyQt5 GUI to view and modify file access and modification timestamps.\n\n## Features\n\n*   **View Timestamps:** Display the current Access, Modification, and Creation times of a selected file.\n    *   Note: Creation time availability and accuracy depend on the operating system and filesystem.\n*   **Modify Timestamps:** Change the Access time (atime) and/or Modification time (mtime) of a file.\n*   **Flexible Time Setting:**\n    *   Set timestamps to the current system time.\n    *   Set timestamps to a specific date and time selected using a calendar widget.\n*   **User Interface:** Simple GUI built with PyQt5.\n*   **Responsiveness:** Uses a background thread for file operations to keep the GUI responsive (though operations are usually very fast).\n*   **Status Updates:** Provides feedback via the status bar and dialog boxes.\n*   **Error Handling:** Reports issues like file not found or permission denied.\n\n**Important Limitation:** Standard Python libraries (`os.utime`) used in this tool can reliably modify **Access** and **Modification** times only. Changing the **Creation** time is highly platform-dependent (e.g., possible on Windows via specific APIs, sometimes possible on macOS/Linux via `utime` with specific flags or external tools) and is **not** implemented in this basic tool.\n\n## Requirements\n\n*   Python 3.x\n*   PyQt5 (`pip install PyQt5`)\n\n## How to Run\n\n1.  Save the following files in the same directory:\n    *   `main.py`\n    *   `file_touch_gui.py`\n    *   `file_touch_logic.py`\n    *   `README.md` (this file)\n2.  Install PyQt5 if you haven't already:\n    ```bash\n    pip install PyQt5\n    ```\n3.  Run the main script:\n    ```bash\n    python main.py\n    ```\n\n## How to Use\n\n1.  **Select File:** Click the **Browse...** button to choose the file whose timestamps you want to view or modify. You can also paste the full path into the text field.\n2.  **View Current Timestamps (Optional):** Click the **Get Current Timestamps** button. The access, modification, and creation times (if available) will be displayed below.\n3.  **Choose Timestamps to Modify:** Check the boxes for **Modify Access Time** and/or **Modify Modification Time**.\n4.  **Select Time Source:**\n    *   Choose **Current System Time** to set the selected timestamp(s) to the time when you click Apply.\n    *   Choose **Specific Date and Time** to activate the date/time editor below it. Select the desired date and time.\n5.  **Apply Changes:** Click the **Apply Timestamps** button.\n6.  **Check Results:** The status bar will indicate success or failure. If successful, the current timestamps display will automatically refresh to show the new values.\n\n"
  }
]
