[
  {
    "filename": "secure_delete_logic.py",
    "content": "import os\nimport time\nimport random\nimport string\nfrom PyQt5.QtCore import QObject, pyqtSignal\n\nCHUNK_SIZE = 1024 * 1024  # 1MB buffer for overwriting\n\nclass SecureDeleteLogic(QObject):\n    \"\"\"Handles the logic for securely deleting (shredding) files.\"\"\"\n    progress_updated = pyqtSignal(int, int, str)  # value, total, message (e.g., pass/total_passes)\n    file_progress = pyqtSignal(int, int)        # bytes_processed, total_bytes (for current pass)\n    operation_complete = pyqtSignal(str)          # success message\n    error_occurred = pyqtSignal(str)            # error message\n    finished = pyqtSignal()                       # signals thread completion\n\n    def __init__(self):\n        super().__init__()\n        self._is_running = False\n        self._filepath = None\n\n    def stop(self):\n        self.progress_updated.emit(0, 1, \"Stopping deletion...\")\n        self._is_running = False\n\n    def _generate_random_bytes(self, length):\n        \"\"\"Generates a block of random bytes.\"\"\"\n        return os.urandom(length)\n\n    def _generate_random_filename(self, length=16):\n        \"\"\"Generates a random filename string.\"\"\"\n        chars = string.ascii_letters + string.digits\n        return ''.join(random.choice(chars) for _ in range(length))\n\n    def shred_file(self, filepath, passes=3):\n        \"\"\"Securely deletes a file by overwriting it multiple times.\"\"\"\n        self._is_running = True\n        self._filepath = filepath\n        current_pass = 0\n\n        try:\n            if not os.path.exists(filepath):\n                raise FileNotFoundError(f\"File not found: {filepath}\")\n            if not os.path.isfile(filepath):\n                raise ValueError(f\"Path is not a file: {filepath}\")\n\n            if passes <= 0:\n                raise ValueError(\"Number of passes must be positive.\")\n\n            file_size = os.path.getsize(filepath)\n            self.progress_updated.emit(0, passes, f\"Starting secure delete for {os.path.basename(filepath)} ({passes} passes)\")\n\n            # --- Overwriting Passes ---\n            for i in range(passes):\n                if not self._is_running: break\n                current_pass = i + 1\n                pass_message = f\"Overwrite Pass {current_pass}/{passes}\"\n                self.progress_updated.emit(current_pass -1 , passes, pass_message)\n                self.file_progress.emit(0, file_size)\n\n                bytes_written_this_pass = 0\n                pattern_type = \"\"\n                \n                # Define patterns (simple approach based on pass number)\n                if i == 0: \n                    pattern = b'\\x00'\n                    pattern_type = \"(Zeros)\"\n                elif i == 1 and passes > 1:\n                    pattern = b'\\xFF'\n                    pattern_type = \"(Ones)\"\n                else:\n                    pattern = None # Indicates random data per chunk\n                    pattern_type = \"(Random)\"\n                \n                self.progress_updated.emit(current_pass -1 , passes, f\"{pass_message} {pattern_type}\")\n\n                try:\n                    # Use 'rb+' to open for reading/writing without truncating\n                    with open(filepath, 'rb+') as f:\n                        while bytes_written_this_pass < file_size:\n                            if not self._is_running: break\n                            \n                            chunk_fill_size = min(CHUNK_SIZE, file_size - bytes_written_this_pass)\n                            \n                            if pattern is not None:\n                                data_to_write = pattern * chunk_fill_size\n                            else:\n                                data_to_write = self._generate_random_bytes(chunk_fill_size)\n                            \n                            f.seek(bytes_written_this_pass)\n                            f.write(data_to_write)\n                            bytes_written_this_pass += len(data_to_write)\n                            self.file_progress.emit(bytes_written_this_pass, file_size)\n                        \n                        if not self._is_running: break\n                        \n                        # Ensure data is physically written (crucial!)\n                        f.flush()\n                        os.fsync(f.fileno())\n                        \n                except Exception as e:\n                    # Catch errors during file access within a pass\n                    raise IOError(f\"Error during overwrite pass {current_pass}: {e}\") from e\n\n            if not self._is_running: \n                self.error_occurred.emit(\"Deletion cancelled during overwriting.\")\n                # State is uncertain, do not proceed with delete/rename\n                self.finished.emit()\n                return\n\n            # --- Rename before final delete (Obscurity) ---\n            self.progress_updated.emit(passes, passes, \"Renaming file...\")\n            new_name = self._generate_random_filename()\n            dir_path = os.path.dirname(filepath)\n            new_filepath = os.path.join(dir_path, new_name)\n            try:\n                # Ensure file is closed before renaming\n                # Rename might fail if handle still open, but 'with open' handles closure.\n                 os.rename(filepath, new_filepath)\n                 self._filepath = new_filepath # Update path for deletion\n                 self.progress_updated.emit(passes, passes, \"File renamed.\")\n            except Exception as e:\n                 # If rename fails, still try to delete original path but warn\n                 self.error_occurred.emit(f\"Warning: Could not rename file before deletion: {e}. Attempting deletion of original name.\")\n\n            # --- Final Deletion ---\n            if not self._is_running: # Check again before final delete\n                 self.error_occurred.emit(\"Deletion cancelled before final removal.\")\n                 # We might have renamed, state is uncertain.\n                 self.finished.emit()\n                 return\n                 \n            self.progress_updated.emit(passes, passes, f\"Deleting file entry: {os.path.basename(self._filepath)}\")\n            try:\n                 os.remove(self._filepath)\n                 self.operation_complete.emit(f\"File securely deleted: {os.path.basename(filepath)} (final name: {os.path.basename(self._filepath)})\")\n            except Exception as e:\n                  raise OSError(f\"Error deleting final file entry '{self._filepath}': {e}\") from e\n\n        except (FileNotFoundError, ValueError, IOError, OSError) as e:\n            if self._is_running: # Don't emit error if stopped\n                self.error_occurred.emit(f\"Error: {e}\")\n        except PermissionError as e:\n             if self._is_running:\n                self.error_occurred.emit(f\"Permission Error: {e}. Check file permissions.\")\n        except Exception as e:\n            if self._is_running:\n                self.error_occurred.emit(f\"An unexpected error occurred: {e}\")\n        finally:\n            self._is_running = False\n            self.finished.emit()\n"
  },
  {
    "filename": "secure_delete_gui.py",
    "content": "import sys\nimport os\nfrom PyQt5.QtWidgets import (\n    QApplication, QMainWindow, QWidget, QVBoxLayout, QHBoxLayout,\n    QPushButton, QLabel, QLineEdit, QFileDialog, QStatusBar,\n    QMessageBox, QProgressBar, QSpinBox, QGroupBox, QSizePolicy\n)\nfrom PyQt5.QtCore import Qt, QThread, pyqtSignal, QObject\n\nfrom secure_delete_logic import SecureDeleteLogic\n\nclass Worker(QObject):\n    \"\"\"Worker thread for secure deletion logic.\"\"\"\n    progress_updated = pyqtSignal(int, int, str)\n    file_progress = pyqtSignal(int, int)\n    operation_complete = pyqtSignal(str)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, logic_instance):\n        super().__init__()\n        self.logic = logic_instance\n        self._filepath = None\n        self._passes = None\n\n    def run(self):\n        # Connect logic signals\n        self.logic.progress_updated.connect(self.progress_updated)\n        self.logic.file_progress.connect(self.file_progress)\n        self.logic.operation_complete.connect(self.operation_complete)\n        self.logic.error_occurred.connect(self.error_occurred)\n        self.logic.finished.connect(self.on_logic_finished)\n\n        if self._filepath and self._passes is not None:\n            self.logic.shred_file(self._filepath, self._passes)\n        else:\n            self.error_occurred.emit(\"Worker started without file path or passes.\")\n            self.on_logic_finished()\n\n    def set_task(self, filepath, passes):\n        self._filepath = filepath\n        self._passes = passes\n\n    def stop(self):\n        self.logic.stop()\n\n    def on_logic_finished(self):\n        # Disconnect signals\n        try:\n            self.logic.progress_updated.disconnect(self.progress_updated)\n            self.logic.file_progress.disconnect(self.file_progress)\n            self.logic.operation_complete.disconnect(self.operation_complete)\n            self.logic.error_occurred.disconnect(self.error_occurred)\n            self.logic.finished.disconnect(self.on_logic_finished)\n        except TypeError:\n            pass # Ignore if already disconnected\n        self.finished.emit()\n\n\nclass SecureDeleteApp(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle(\"Secure File Shredder\")\n        self.setGeometry(100, 100, 550, 350)\n\n        self.central_widget = QWidget()\n        self.setCentralWidget(self.central_widget)\n        self.main_layout = QVBoxLayout(self.central_widget)\n\n        # --- Worker Thread Setup ---\n        self.thread = None\n        self.worker = None\n        self.logic = SecureDeleteLogic()\n\n        # --- File Selection ---\n        file_layout = QHBoxLayout()\n        file_label = QLabel(\"File to Shred:\")\n        self.file_edit = QLineEdit()\n        self.file_edit.setPlaceholderText(\"Select a file to securely delete...\")\n        self.file_edit.setReadOnly(True)\n        self.browse_button = QPushButton(\"Browse...\")\n        self.browse_button.clicked.connect(self.browse_file)\n        file_layout.addWidget(file_label)\n        file_layout.addWidget(self.file_edit)\n        file_layout.addWidget(self.browse_button)\n        self.main_layout.addLayout(file_layout)\n\n        # --- Options ---\n        options_group = QGroupBox(\"Shredding Options\")\n        options_layout = QHBoxLayout()\n        passes_label = QLabel(\"Overwrite Passes:\")\n        self.passes_spinbox = QSpinBox()\n        self.passes_spinbox.setRange(1, 35) # 1 to Gutmann-ish max, practical limit lower\n        self.passes_spinbox.setValue(3) # Default to 3 passes\n        self.passes_spinbox.setToolTip(\"Number of times to overwrite the file data (e.g., 1, 3, 7). More passes take longer.\")\n        options_layout.addWidget(passes_label)\n        options_layout.addWidget(self.passes_spinbox)\n        options_layout.addStretch()\n        options_group.setLayout(options_layout)\n        self.main_layout.addWidget(options_group)\n        \n        # --- Warning --- \n        warning_label = QLabel(\n            \"<font color='red'><b>Warning:</b> Secure deletion is irreversible! \"\n            \"The selected file will be permanently destroyed. \"\n            \"Effectiveness on SSDs may vary.</font>\"\n        )\n        warning_label.setWordWrap(True)\n        self.main_layout.addWidget(warning_label)\n\n        # --- Action Button ---\n        self.shred_button = QPushButton(\"Securely Delete File\")\n        self.shred_button.setStyleSheet(\"background-color: #FFCCCC; color: black;\") # Light red background\n        self.shred_button.clicked.connect(self.start_shred_task)\n        self.shred_button.setEnabled(False) # Enabled when file selected\n        self.main_layout.addWidget(self.shred_button)\n        \n        self.main_layout.addStretch()\n\n        # --- Progress Bar ---\n        self.pass_progress_label = QLabel(\"Pass Progress:\")\n        self.pass_progress_bar = QProgressBar()\n        self.pass_progress_bar.setTextVisible(True)\n        self.pass_progress_bar.setValue(0)\n        self.main_layout.addWidget(self.pass_progress_label)\n        self.main_layout.addWidget(self.pass_progress_bar)\n        \n        self.file_progress_label = QLabel(\"File Overwrite Progress (Current Pass):\")\n        self.file_progress_bar = QProgressBar()\n        self.file_progress_bar.setTextVisible(True)\n        self.file_progress_bar.setValue(0)\n        self.main_layout.addWidget(self.file_progress_label)\n        self.main_layout.addWidget(self.file_progress_bar)\n        \n        # Initially hide progress bars\n        self.pass_progress_label.setVisible(False)\n        self.pass_progress_bar.setVisible(False)\n        self.file_progress_label.setVisible(False)\n        self.file_progress_bar.setVisible(False)\n\n        # --- Status Bar ---\n        self.status_bar = QStatusBar()\n        self.setStatusBar(self.status_bar)\n        self.status_bar.showMessage(\"Ready. Select a file to shred.\")\n\n        # --- Stop Button ---\n        self.stop_button = QPushButton(\"Stop Deletion\")\n        self.stop_button.setEnabled(False)\n        self.stop_button.clicked.connect(self.stop_task)\n        self.main_layout.addWidget(self.stop_button)\n\n    def browse_file(self):\n        filepath, _ = QFileDialog.getOpenFileName(self, \"Select File to Securely Delete\")\n        if filepath:\n            self.file_edit.setText(filepath)\n            self.shred_button.setEnabled(True)\n            self.status_bar.showMessage(f\"Selected: {filepath}. Ready to shred.\")\n        else:\n             self.shred_button.setEnabled(False)\n\n    def start_shred_task(self):\n        filepath = self.file_edit.text()\n        passes = self.passes_spinbox.value()\n\n        if not filepath or not os.path.isfile(filepath):\n            QMessageBox.warning(self, \"Invalid File\", \"Please select a valid file to shred.\")\n            return\n            \n        # CRITICAL CONFIRMATION\n        confirm_msg = (f\"Are you absolutely sure you want to permanently shred the file:\\n\\n\"\n                       f\"'{filepath}'\\n\\n\"\n                       f\"This action cannot be undone and the file data will be overwritten {passes} times.\")\n                       \n        reply = QMessageBox.critical(self, \"Confirm Secure Deletion\", confirm_msg,\n                                     QMessageBox.Yes | QMessageBox.Cancel, QMessageBox.Cancel)\n\n        if reply == QMessageBox.Cancel:\n            self.status_bar.showMessage(\"Shred operation cancelled by user.\")\n            return\n\n        # Check if worker already running\n        if self.thread and self.thread.isRunning():\n            QMessageBox.warning(self, \"Busy\", \"A shred operation is already in progress.\")\n            return\n\n        # Prepare and start worker\n        self.status_bar.showMessage(f\"Preparing to shred {os.path.basename(filepath)}...\")\n        self.toggle_controls(False)\n        self.pass_progress_bar.setValue(0)\n        self.pass_progress_bar.setRange(0, passes)\n        self.file_progress_bar.setValue(0)\n        self.file_progress_bar.setRange(0, 100) # Will be updated with actual size\n        self.pass_progress_label.setVisible(True)\n        self.pass_progress_bar.setVisible(True)\n        self.file_progress_label.setVisible(True)\n        self.file_progress_bar.setVisible(True)\n\n        self.thread = QThread()\n        self.worker = Worker(self.logic)\n        self.worker.moveToThread(self.thread)\n\n        # Connect signals\n        self.worker.progress_updated.connect(self.update_pass_progress)\n        self.worker.file_progress.connect(self.update_file_progress)\n        self.worker.operation_complete.connect(self.on_operation_complete)\n        self.worker.error_occurred.connect(self.on_operation_error)\n        self.worker.finished.connect(self.on_task_finished)\n        \n        self.worker.set_task(filepath, passes)\n        self.thread.started.connect(self.worker.run)\n        self.thread.start()\n\n    # --- UI Update Slots ---\n    def update_pass_progress(self, value, total, message):\n        self.status_bar.showMessage(message)\n        if total > 0:\n            if self.pass_progress_bar.maximum() != total:\n                self.pass_progress_bar.setRange(0, total)\n            self.pass_progress_bar.setValue(value)\n            percent = int((value / total) * 100) if value < total else 100 # Show 100% only at end\n            self.pass_progress_bar.setFormat(f\"Pass {value}/{total} - {percent}%\")\n        else:\n            self.pass_progress_bar.setRange(0, 0) # Indeterminate\n            self.pass_progress_bar.setFormat(message)\n        # Reset file progress at the start of a new pass message\n        if \"Pass\" in message and \"Overwrite\" in message:\n            self.file_progress_bar.setValue(0)\n            self.file_progress_bar.setFormat(\"0%\")\n            \n    def update_file_progress(self, value, total):\n        if total > 0:\n             if self.file_progress_bar.maximum() != total:\n                  self.file_progress_bar.setRange(0, total)\n             self.file_progress_bar.setValue(value)\n             percent = int((value / total) * 100)\n             self.file_progress_bar.setFormat(f\"{percent}%\")\n        else: # Handle zero-byte file case\n             self.file_progress_bar.setRange(0, 1)\n             self.file_progress_bar.setValue(1)\n             self.file_progress_bar.setFormat(\"100%\")\n\n    def on_operation_complete(self, message):\n        self.status_bar.showMessage(message, 10000) # Show for 10s\n        QMessageBox.information(self, \"Operation Successful\", message)\n        self.pass_progress_bar.setValue(self.pass_progress_bar.maximum()) # Ensure pass bar full\n        self.file_progress_bar.setValue(self.file_progress_bar.maximum()) # Ensure file bar full\n        self.pass_progress_bar.setFormat(\"Complete!\")\n        self.file_progress_bar.setFormat(\"Complete!\")\n        self.file_edit.clear() # Clear selection after success\n        self.shred_button.setEnabled(False)\n        # Wait for finished signal to re-enable controls and hide progress\n\n    def on_operation_error(self, message):\n        self.status_bar.showMessage(f\"Error: {message}\")\n        QMessageBox.critical(self, \"Operation Failed\", message)\n        self.pass_progress_bar.setFormat(\"Failed!\")\n        self.file_progress_bar.setFormat(\"Failed!\")\n        # Don't clear selection on error, user might want to retry / check permissions\n        # Wait for finished signal to re-enable controls and hide progress\n\n    def on_task_finished(self):\n        if self.thread is None: return # Safety\n        \n        self.thread.quit()\n        self.thread.wait()\n        self.thread = None\n        self.worker = None\n        self.toggle_controls(True)\n        # Hide progress bars after completion/error/stop\n        self.pass_progress_label.setVisible(False)\n        self.pass_progress_bar.setVisible(False)\n        self.file_progress_label.setVisible(False)\n        self.file_progress_bar.setVisible(False)\n        # Keep final status message\n\n    def stop_task(self):\n        if self.worker:\n             self.status_bar.showMessage(\"Attempting to stop shredding...\")\n             self.worker.stop()\n             self.stop_button.setEnabled(False) # Disable stop button immediately\n             # Progress bars will be hidden in on_task_finished\n\n    def toggle_controls(self, enable):\n        self.browse_button.setEnabled(enable)\n        # Only enable shred button if a file is selected AND controls are enabled\n        self.shred_button.setEnabled(enable and bool(self.file_edit.text()))\n        self.passes_spinbox.setEnabled(enable)\n        self.stop_button.setEnabled(not enable)\n\n    def closeEvent(self, event):\n        if self.thread is not None and self.thread.isRunning():\n            reply = QMessageBox.question(self, \"Confirm Exit\", \
                                         \"A shred operation is in progress. Stop it and exit?\",\n                                         QMessageBox.Yes | QMessageBox.No, QMessageBox.No)\n            if reply == QMessageBox.Yes:\n                self.stop_task()\n                # Give a short time for stop signal to potentially process\n                self.thread.quit()\n                self.thread.wait(1000) # Shredding can take time, allow 1s \n                event.accept()\n            else:\
                event.ignore()\n        else:\
            event.accept()\n\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    main_win = SecureDeleteApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "main.py",
    "content": "import sys\nfrom PyQt5.QtWidgets import QApplication\nfrom PyQt5.QtCore import Qt\nfrom secure_delete_gui import SecureDeleteApp\n\nif __name__ == '__main__':\n    # Enable high DPI scaling for better visuals on some systems\n    if hasattr(QApplication, 'setAttribute'):\n        QApplication.setAttribute(Qt.AA_EnableHighDpiScaling, True)\n        QApplication.setAttribute(Qt.AA_UseHighDpiPixmaps, True)\n\n    app = QApplication(sys.argv)\n    main_win = SecureDeleteApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "README.md",
    "content": "# Secure File Shredder\n\nA Python tool with a PyQt5 GUI to securely delete files by overwriting their content multiple times before removing them from the filesystem.\n\n## Features\n\n*   **Secure Overwriting:** Overwrites the entire file content with multiple passes of data (zeros, ones, random data) before deletion.\n*   **Configurable Passes:** Choose the number of overwrite passes (default is 3). More passes increase security theatre but take significantly longer.\n*   **Obscurity:** Renames the file with random characters before the final deletion step to obscure the original filename (if possible).\n*   **User Interface:** Simple GUI built with PyQt5.\n*   **Responsiveness:** Uses a separate thread for the time-consuming shredding process to keep the GUI responsive.\n*   **Progress Indication:** Shows progress per pass and detailed file overwrite progress using progress bars and status messages.\n*   **Cancellation:** Allows stopping an ongoing shred operation.\n*   **Confirmation:** Requires explicit user confirmation before starting the irreversible deletion process.\n*   **Error Handling:** Reports issues like file not found, permission errors, or errors during overwriting/deletion.\n\n**Important Considerations:**\n\n*   **Irreversible Action:** Shredding is permanent. Once a file is shredded using this tool, its original data cannot be recovered through normal means. **Use with extreme caution!**\n*   **SSD Effectiveness:** Due to wear leveling and garbage collection mechanisms in Solid State Drives (SSDs), multiple overwrites may not be as effective as on traditional Hard Disk Drives (HDDs) and can contribute to drive wear. A single pass of random data is often considered sufficient for SSDs. This tool performs the requested passes regardless of drive type.\n*   **Filesystem Artifacts:** This tool focuses on overwriting the file's data content. It may not erase all traces of the file's existence from filesystem metadata, journals, or slack space. Forensic recovery might still be possible in some scenarios.\n*   **Permissions:** The tool requires write permissions for the file and its containing directory (for renaming and deletion).\n\n## Requirements\n\n*   Python 3.x\n*   PyQt5 (`pip install PyQt5`)\n\n## How to Run\n\n1.  Save the following files in the same directory:\n    *   `main.py`\n    *   `secure_delete_gui.py`\n    *   `secure_delete_logic.py`\n    *   `README.md` (this file)\n2.  Install PyQt5 if you haven't already:\n    ```bash\n    pip install PyQt5\n    ```\n3.  Run the main script:\n    ```bash\n    python main.py\n    ```\n\n## How to Use\n\n1.  **Select File:** Click the **Browse...** button and choose the file you want to securely delete.\n2.  **Choose Passes:** Select the desired number of overwrite passes using the spin box (default is 3). Consider the trade-off between time and theoretical security.\n3.  **Review Warning:** Read the warning message carefully.\n4.  **Initiate Shredding:** Click the **Securely Delete File** button.\n5.  **Confirm:** A critical confirmation dialog will appear. Read it carefully. Click **Yes** only if you are absolutely sure you want to permanently destroy the file.\n6.  **Monitor Progress:** The progress bars and status bar will show the progress of the overwrite passes and file deletion.\n7.  **Stop Operation:** If you need to cancel the process, click the **Stop Deletion** button (cancellation might take a moment, and the file state will be uncertain).\n8.  **Completion:** A message will indicate whether the operation was successful or failed.\n"
  }
]
