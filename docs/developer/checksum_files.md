[
  {
    "filename": "checksum_logic.py",
    "content": "import hashlib\nimport os\nfrom PyQt5.QtCore import QObject, pyqtSignal\n\nVALID_ALGORITHMS = ['md5', 'sha1', 'sha256', 'sha512']\nCHUNK_SIZE = 8192  # Read file in chunks\n\nclass ChecksumLogic(QObject):\n    \"\"\"Handles checksum calculation and verification logic.\"\"\"\n    progress_updated = pyqtSignal(int, int)  # current, total\n    result_ready = pyqtSignal(dict) # Results dictionary\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, target_path, algorithm, expected_checksum=None, checksum_file_path=None, mode='calculate'):\n        super().__init__()\n        if algorithm.lower() not in VALID_ALGORITHMS:\n            raise ValueError(f\"Invalid algorithm: {algorithm}. Choose from {VALID_ALGORITHMS}\")\n\n        self.target_path = target_path\n        self.algorithm = algorithm.lower()\n        self.expected_checksum = expected_checksum\n        self.checksum_file_path = checksum_file_path\n        self.mode = mode # 'calculate_file', 'calculate_dir', 'verify_file', 'verify_dir'\n        self._is_running = True\n\n    def stop(self):\n        self._is_running = False\n\n    def run(self):\n        \"\"\"Main execution method to be run in a thread.\"\"\"\n        try:\n            self._is_running = True\n            results = {}\n            if self.mode == 'calculate_file':\n                checksum = self._calculate_file_checksum(self.target_path)\n                if checksum:\n                    results[self.target_path] = checksum\n            elif self.mode == 'calculate_dir':\n                results = self._calculate_directory_checksums(self.target_path)\n            elif self.mode == 'verify_file':\n                status = self._verify_file_checksum(self.target_path, self.expected_checksum)\n                results[self.target_path] = status\n            elif self.mode == 'verify_dir':\n                results = self._verify_directory_checksums(self.target_path, self.checksum_file_path)\n            else:\n                 raise ValueError(f\"Invalid mode: {self.mode}\")\n\n            if self._is_running:\n                self.result_ready.emit(results)\n\n        except Exception as e:\n            if self._is_running: # Don't emit error if stopped manually\n                self.error_occurred.emit(f\"Error: {e}\")\n        finally:\n            if self._is_running:\n                self.finished.emit()\n\n    def _calculate_file_checksum(self, file_path):\n        \"\"\"Calculates the checksum for a single file.\"\"\"\n        if not self._is_running: return None\n        hasher = hashlib.new(self.algorithm)\n        try:\n            with open(file_path, 'rb') as f:\n                total_size = os.path.getsize(file_path)\n                bytes_read = 0\n                while self._is_running:\n                    chunk = f.read(CHUNK_SIZE)\n                    if not chunk:\n                        break\n                    hasher.update(chunk)\n                    bytes_read += len(chunk)\n                    # Emit progress infrequently to avoid overwhelming GUI thread\n                    if bytes_read % (CHUNK_SIZE * 50) == 0 or bytes_read == total_size:\n                         self.progress_updated.emit(bytes_read, total_size)\n                if not self._is_running: return None\n                return hasher.hexdigest()\n        except FileNotFoundError:\n            self.error_occurred.emit(f\"File not found: {file_path}\")\n            return None\n        except PermissionError:\n            self.error_occurred.emit(f\"Permission denied: {file_path}\")\n            return None\n        except Exception as e:\n            self.error_occurred.emit(f\"Error reading {file_path}: {e}\")\n            return None\n\n    def _calculate_directory_checksums(self, dir_path):\n        \"\"\"Calculates checksums for all files in a directory recursively.\"\"\"\n        results = {}\n        files_to_process = []\n        for root, _, files in os.walk(dir_path):\n            if not self._is_running: break\n            for filename in files:\n                 if not self._is_running: break\n                 file_path = os.path.join(root, filename)\n                 files_to_process.append(file_path)\n            if not self._is_running: break\n\n        total_files = len(files_to_process)\n        processed_files = 0\n        for file_path in files_to_process:\n            if not self._is_running: break\n            checksum = self._calculate_file_checksum(file_path) # Progress updated within this func\n            if checksum:\n                relative_path = os.path.relpath(file_path, dir_path)\n                results[relative_path] = checksum\n            processed_files += 1\n            # Update overall directory progress (can be simplified)\n            # self.progress_updated.emit(processed_files, total_files)\n\n        return results\n\n    def _verify_file_checksum(self, file_path, expected_checksum):\n        \"\"\"Verifies a single file against an expected checksum.\"\"\"\n        if not expected_checksum:\n            return \"ERROR: No expected checksum provided.\"\n        calculated_checksum = self._calculate_file_checksum(file_path)\n        if calculated_checksum is None:\n             return \"ERROR: Could not calculate checksum.\"\n        if calculated_checksum.lower() == expected_checksum.lower():\n            return \"OK\"\n        else:\n            return f\"MISMATCH (Expected: {expected_checksum}, Got: {calculated_checksum})\"\n\n    def _parse_checksum_file(self, checksum_file_path):\n        \"\"\"Parses a checksum file (e.g., sha256sum output format).\"\"\"\n        expected_checksums = {}\n        try:\n            with open(checksum_file_path, 'r') as f:\n                for line in f:\n                    if not self._is_running: break\n                    line = line.strip()\n                    if not line or line.startswith('#'):\n                        continue\n                    parts = line.split(None, 1) # Split on first whitespace\n                    if len(parts) == 2:\n                        checksum, filename = parts\n                        # Handle potential path separators (* for binary mode in sha*sum)\n                        filename = filename.lstrip('*').strip()\n                        expected_checksums[filename] = checksum\n                    else:\n                         self.error_occurred.emit(f\"Skipping malformed line in {checksum_file_path}: {line}\")\n        except FileNotFoundError:\n            self.error_occurred.emit(f\"Checksum file not found: {checksum_file_path}\")\n            return None\n        except Exception as e:\n            self.error_occurred.emit(f\"Error reading checksum file {checksum_file_path}: {e}\")\n            return None\n        return expected_checksums\n\n    def _verify_directory_checksums(self, dir_path, checksum_file_path):\n        \"\"\"Verifies files in a directory against a checksum file.\"\"\"\n        results = {}\n        expected_checksums = self._parse_checksum_file(checksum_file_path)\n        if expected_checksums is None:\n            return {checksum_file_path: \"ERROR: Failed to parse checksum file.\"}\n\n        processed_files = set()\n        files_in_dir = []\n        for root, _, files in os.walk(dir_path):\n             if not self._is_running: break\n             for filename in files:\n                  if not self._is_running: break\n                  full_path = os.path.join(root, filename)\n                  relative_path = os.path.relpath(full_path, dir_path).replace('\\\\', '/') # Normalize path sep\n                  files_in_dir.append((full_path, relative_path))\n             if not self._is_running: break\n\n        total_files = len(files_in_dir)\n        checked_count = 0\n\n        for full_path, relative_path in files_in_dir:\n            if not self._is_running: break\n            processed_files.add(relative_path)\n            expected_checksum = expected_checksums.get(relative_path)\n\n            if expected_checksum:\n                status = self._verify_file_checksum(full_path, expected_checksum)\n                results[relative_path] = status\n            else:\n                results[relative_path] = \"WARNING: Not found in checksum file.\"\n\n            checked_count += 1\n            # Update overall progress (can be simplified)\n            # self.progress_updated.emit(checked_count, total_files)\n\n        # Check for files listed in checksum file but not found in directory\n        if self._is_running:\n            for relative_path in expected_checksums:\n                if relative_path not in processed_files:\n                    results[relative_path] = \"ERROR: File listed in checksum file but not found in directory.\"\n\n        return results\n"
  },
  {
    "filename": "checksum_gui.py",
    "content": "import sys\nimport os\nfrom PyQt5.QtWidgets import (\n    QApplication, QMainWindow, QWidget, QVBoxLayout, QHBoxLayout,\n    QPushButton, QLabel, QLineEdit, QComboBox, QTextEdit,\n    QFileDialog, QProgressBar, QStatusBar, QTabWidget, QRadioButton,\n    QGroupBox, QMessageBox\n)\nfrom PyQt5.QtCore import Qt, QThread, pyqtSignal\n\nfrom checksum_logic import ChecksumLogic, VALID_ALGORITHMS\n\nclass Worker(QObject):\n    \"\"\"Worker object to run ChecksumLogic in a separate thread.\"\"\"\n    progress_updated = pyqtSignal(int, int)\n    result_ready = pyqtSignal(dict)\n    error_occurred = pyqtSignal(str)\n    finished = pyqtSignal()\n\n    def __init__(self, logic_instance):\n        super().__init__()\n        self.logic = logic_instance\n\n    def run(self):\n        # Connect signals from logic to worker signals\n        self.logic.progress_updated.connect(self.progress_updated)\n        self.logic.result_ready.connect(self.result_ready)\n        self.logic.error_occurred.connect(self.error_occurred)\n        self.logic.finished.connect(self.finished)\n        self.logic.run()\n\n    def stop(self):\n        self.logic.stop()\n\nclass ChecksumApp(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle(\"Checksum Generator/Validator\")\n        self.setGeometry(100, 100, 700, 500)\n\n        self.central_widget = QWidget()\n        self.setCentralWidget(self.central_widget)\n        self.layout = QVBoxLayout(self.central_widget)\n\n        self.tabs = QTabWidget()\n        self.layout.addWidget(self.tabs)\n\n        # --- Worker Thread Setup ---\n        self.thread = None\n        self.worker = None\n        self.logic_instance = None\n\n        # --- Create Tabs ---\n        self.create_calculate_tab()\n        self.create_verify_tab()\n\n        # --- Progress Bar and Status Bar ---\n        self.progress_bar = QProgressBar()\n        self.progress_bar.setTextVisible(True)\n        self.progress_bar.setValue(0)\n        self.layout.addWidget(self.progress_bar)\n\n        self.status_bar = QStatusBar()\n        self.setStatusBar(self.status_bar)\n        self.status_bar.showMessage(\"Ready\")\n\n        # --- Stop Button ---\n        self.stop_button = QPushButton(\"Stop Operation\")\n        self.stop_button.setEnabled(False)\n        self.stop_button.clicked.connect(self.stop_operation)\n        self.layout.addWidget(self.stop_button)\n\n    def create_calculate_tab(self):\n        self.calculate_tab = QWidget()\n        self.tabs.addTab(self.calculate_tab, \"Calculate Checksum\")\n        layout = QVBoxLayout(self.calculate_tab)\n\n        # Input Type Selection\n        input_group = QGroupBox(\"Select Input Type\")\n        input_layout = QHBoxLayout()\n        self.calc_radio_file = QRadioButton(\"File\")\n        self.calc_radio_dir = QRadioButton(\"Directory\")\n        self.calc_radio_file.setChecked(True)\n        input_layout.addWidget(self.calc_radio_file)\n        input_layout.addWidget(self.calc_radio_dir)\n        input_group.setLayout(input_layout)\n        layout.addWidget(input_group)\n\n        # File/Directory Selection\n        select_layout = QHBoxLayout()\n        self.calc_path_label = QLabel(\"Selected Item:\")\n        self.calc_path_edit = QLineEdit()\n        self.calc_path_edit.setReadOnly(True)\n        self.calc_select_button = QPushButton(\"Select File...\")\n        self.calc_select_button.clicked.connect(self.select_calc_item)\n        select_layout.addWidget(self.calc_path_label)\n        select_layout.addWidget(self.calc_path_edit)\n        select_layout.addWidget(self.calc_select_button)\n        layout.addLayout(select_layout)\n\n        # Connect radio buttons to update button text\n        self.calc_radio_file.toggled.connect(lambda: self.calc_select_button.setText(\"Select File...\"))\n        self.calc_radio_dir.toggled.connect(lambda: self.calc_select_button.setText(\"Select Directory...\"))\n\n        # Algorithm Selection\n        algo_layout = QHBoxLayout()\n        algo_label = QLabel(\"Algorithm:\")\n        self.calc_algo_combo = QComboBox()\n        self.calc_algo_combo.addItems(VALID_ALGORITHMS)\n        algo_layout.addWidget(algo_label)\n        algo_layout.addWidget(self.calc_algo_combo)\n        layout.addLayout(algo_layout)\n\n        # Calculate Button\n        self.calc_button = QPushButton(\"Calculate Checksum\")\n        self.calc_button.clicked.connect(self.start_calculation)\n        layout.addWidget(self.calc_button)\n\n        # Results Area\n        result_label = QLabel(\"Results:\")\n        self.calc_result_text = QTextEdit()\n        self.calc_result_text.setReadOnly(True)\n        layout.addWidget(result_label)\n        layout.addWidget(self.calc_result_text)\n\n    def create_verify_tab(self):\n        self.verify_tab = QWidget()\n        self.tabs.addTab(self.verify_tab, \"Verify Checksum\")\n        layout = QVBoxLayout(self.verify_tab)\n\n        # Verification Type Selection\n        verify_type_group = QGroupBox(\"Select Verification Type\")\n        verify_type_layout = QHBoxLayout()\n        self.verify_radio_file = QRadioButton(\"Single File\")\n        self.verify_radio_dir = QRadioButton(\"Directory (using checksum file)\")\n        self.verify_radio_file.setChecked(True)\n        verify_type_layout.addWidget(self.verify_radio_file)\n        verify_type_layout.addWidget(self.verify_radio_dir)\n        verify_type_group.setLayout(verify_type_layout)\n        layout.addWidget(verify_type_group)\n\n        # Target File/Directory Selection\n        target_layout = QHBoxLayout()\n        self.verify_target_label = QLabel(\"File to Verify:\") # Changes dynamically\n        self.verify_target_edit = QLineEdit()\n        self.verify_target_edit.setReadOnly(True)\n        self.verify_select_target_button = QPushButton(\"Select File...\")\n        self.verify_select_target_button.clicked.connect(self.select_verify_target)\n        target_layout.addWidget(self.verify_target_label)\n        target_layout.addWidget(self.verify_target_edit)\n        target_layout.addWidget(self.verify_select_target_button)\n        layout.addLayout(target_layout)\n\n        # Expected Checksum / Checksum File Input\n        self.verify_checksum_group = QGroupBox(\"Checksum Information\") # Container\n        self.verify_checksum_layout = QVBoxLayout()\n        self.verify_checksum_group.setLayout(self.verify_checksum_layout)\n\n        # -- Single File Widgets --\n        self.verify_single_file_widgets = QWidget()\n        single_file_layout = QHBoxLayout(self.verify_single_file_widgets)\n        single_file_layout.setContentsMargins(0,0,0,0)\n        self.verify_expected_label = QLabel(\"Expected Checksum:\")\n        self.verify_expected_edit = QLineEdit()\n        single_file_layout.addWidget(self.verify_expected_label)\n        single_file_layout.addWidget(self.verify_expected_edit)\n\n        # -- Directory Widgets --\n        self.verify_dir_widgets = QWidget()\n        dir_layout = QHBoxLayout(self.verify_dir_widgets)\n        dir_layout.setContentsMargins(0,0,0,0)\n        self.verify_checksum_file_label = QLabel(\"Checksum File:\")\n        self.verify_checksum_file_edit = QLineEdit()\n        self.verify_checksum_file_edit.setReadOnly(True)\n        self.verify_select_checksum_file_button = QPushButton(\"Select Checksum File...\")\n        self.verify_select_checksum_file_button.clicked.connect(self.select_checksum_file)\n        dir_layout.addWidget(self.verify_checksum_file_label)\n        dir_layout.addWidget(self.verify_checksum_file_edit)\n        dir_layout.addWidget(self.verify_select_checksum_file_button)\n\n        # Add initial widget set\n        self.verify_checksum_layout.addWidget(self.verify_single_file_widgets)\n        self.verify_dir_widgets.setVisible(False) # Hide dir widgets initially\n        self.verify_checksum_layout.addWidget(self.verify_dir_widgets)\n        layout.addWidget(self.verify_checksum_group)\n\n        # Connect radio buttons to change UI\n        self.verify_radio_file.toggled.connect(self.toggle_verify_mode)\n        self.verify_radio_dir.toggled.connect(self.toggle_verify_mode)\n\n        # Algorithm Selection (Common for both modes)\n        verify_algo_layout = QHBoxLayout()\n        verify_algo_label = QLabel(\"Algorithm (must match expected checksum/file):\")\n        self.verify_algo_combo = QComboBox()\n        self.verify_algo_combo.addItems(VALID_ALGORITHMS)\n        verify_algo_layout.addWidget(verify_algo_label)\n        verify_algo_layout.addWidget(self.verify_algo_combo)\n        layout.addLayout(verify_algo_layout)\n\n        # Verify Button\n        self.verify_button = QPushButton(\"Verify Checksum\")\n        self.verify_button.clicked.connect(self.start_verification)\n        layout.addWidget(self.verify_button)\n\n        # Results Area\n        verify_result_label = QLabel(\"Verification Results:\")\n        self.verify_result_text = QTextEdit()\n        self.verify_result_text.setReadOnly(True)\n        layout.addWidget(verify_result_label)\n        layout.addWidget(self.verify_result_text)\n\n    def toggle_verify_mode(self):\n        is_file_mode = self.verify_radio_file.isChecked()\n\n        self.verify_target_label.setText(\"File to Verify:\" if is_file_mode else \"Directory to Verify:\")\n        self.verify_select_target_button.setText(\"Select File...\" if is_file_mode else \"Select Directory...\")\n        self.verify_single_file_widgets.setVisible(is_file_mode)\n        self.verify_dir_widgets.setVisible(not is_file_mode)\n        # Clear fields when switching modes\n        self.verify_target_edit.clear()\n        self.verify_expected_edit.clear()\n        self.verify_checksum_file_edit.clear()\n        self.verify_result_text.clear()\n\n\n    # --- Slot Methods ---\n    def select_calc_item(self):\n        if self.calc_radio_file.isChecked():\n            filepath, _ = QFileDialog.getOpenFileName(self, \"Select File\")\n            if filepath:\n                self.calc_path_edit.setText(filepath)\n        else:\n            dirpath = QFileDialog.getExistingDirectory(self, \"Select Directory\")\n            if dirpath:\n                self.calc_path_edit.setText(dirpath)\n\n    def select_verify_target(self):\n        if self.verify_radio_file.isChecked():\n            filepath, _ = QFileDialog.getOpenFileName(self, \"Select File to Verify\")\n            if filepath:\n                self.verify_target_edit.setText(filepath)\n        else:\n            dirpath = QFileDialog.getExistingDirectory(self, \"Select Directory to Verify\")\n            if dirpath:\n                self.verify_target_edit.setText(dirpath)\n\n    def select_checksum_file(self):\n        filepath, _ = QFileDialog.getOpenFileName(self, \"Select Checksum File\")\n        if filepath:\n            self.verify_checksum_file_edit.setText(filepath)\n\n    def start_calculation(self):\n        target_path = self.calc_path_edit.text()\n        algorithm = self.calc_algo_combo.currentText()\n        is_file = self.calc_radio_file.isChecked()\n\n        if not target_path:\n            QMessageBox.warning(self, \"Input Missing\", \"Please select a file or directory.\")\n            return\n        if not os.path.exists(target_path):\n             QMessageBox.warning(self, \"Input Error\", f\"Path does not exist: {target_path}\")\n             return\n\n        mode = 'calculate_file' if is_file else 'calculate_dir'\n        if is_file and not os.path.isfile(target_path):\n             QMessageBox.warning(self, \"Input Error\", f\"Selected path is not a file: {target_path}\")\n             return\n        if not is_file and not os.path.isdir(target_path):\n             QMessageBox.warning(self, \"Input Error\", f\"Selected path is not a directory: {target_path}\")\n             return\n\n        self.calc_result_text.clear()\n        self.status_bar.showMessage(f\"Calculating {algorithm.upper()} for {os.path.basename(target_path)}...\")\n        self.progress_bar.setRange(0, 0) # Indeterminate progress initially\n        self.toggle_controls(enabled=False)\n\n        self.logic_instance = ChecksumLogic(target_path, algorithm, mode=mode)\n        self.run_worker(self.logic_instance)\n\n    def start_verification(self):\n        target_path = self.verify_target_edit.text()\n        algorithm = self.verify_algo_combo.currentText()\n        is_file_mode = self.verify_radio_file.isChecked()\n\n        if not target_path:\n            QMessageBox.warning(self, \"Input Missing\", \"Please select the target file or directory.\")\n            return\n        if not os.path.exists(target_path):\n            QMessageBox.warning(self, \"Input Error\", f\"Target path does not exist: {target_path}\")\n            return\n\n        mode = ''\n        expected_checksum = None\n        checksum_file_path = None\n\n        if is_file_mode:\n            mode = 'verify_file'\n            if not os.path.isfile(target_path):\n                QMessageBox.warning(self, \"Input Error\", f\"Selected target is not a file: {target_path}\")\n                return\n            expected_checksum = self.verify_expected_edit.text().strip()\n            if not expected_checksum:\n                QMessageBox.warning(self, \"Input Missing\", \"Please enter the expected checksum.\")\n                return\n        else: # Directory mode\n            mode = 'verify_dir'\n            if not os.path.isdir(target_path):\n                QMessageBox.warning(self, \"Input Error\", f\"Selected target is not a directory: {target_path}\")\n                return\n            checksum_file_path = self.verify_checksum_file_edit.text()\n            if not checksum_file_path:\n                QMessageBox.warning(self, \"Input Missing\", \"Please select the checksum file.\")\n                return\n            if not os.path.isfile(checksum_file_path):\n                 QMessageBox.warning(self, \"Input Error\", f\"Checksum file does not exist or is not a file: {checksum_file_path}\")\n                 return\n\n        self.verify_result_text.clear()\n        self.status_bar.showMessage(f\"Verifying {os.path.basename(target_path)} using {algorithm.upper()}...\")\n        self.progress_bar.setRange(0, 0)\n        self.toggle_controls(enabled=False)\n\n        self.logic_instance = ChecksumLogic(target_path, algorithm, expected_checksum, checksum_file_path, mode)\n        self.run_worker(self.logic_instance)\n\n    def run_worker(self, logic_instance):\n        if self.thread is not None and self.thread.isRunning():\n            # Should not happen if UI is disabled correctly, but good practice\n            print(\"Warning: Worker thread is already running.\")\n            return\n\n        self.thread = QThread()\n        self.worker = Worker(logic_instance)\n        self.worker.moveToThread(self.thread)\n\n        # Connect signals\n        self.worker.progress_updated.connect(self.update_progress)\n        self.worker.result_ready.connect(self.handle_results)\n        self.worker.error_occurred.connect(self.handle_error)\n        self.worker.finished.connect(self.on_worker_finished)\n        self.thread.started.connect(self.worker.run)\n\n        self.thread.start()\n\n    def stop_operation(self):\n        self.status_bar.showMessage(\"Stopping operation...\")\n        if self.worker:\n            self.worker.stop()\n        # Note: The thread might take a moment to finish after stop() is called\n        # We handle UI re-enabling in on_worker_finished\n        self.stop_button.setEnabled(False) # Prevent multiple clicks\n\n    def update_progress(self, current, total):\n        if total > 0:\n            self.progress_bar.setRange(0, total)\n            self.progress_bar.setValue(current)\n            percent = int((current / total) * 100)\n            self.status_bar.showMessage(f\"Processing... {percent}%\")\n        else:\n            # For indeterminate stages or simple file ops\n            self.progress_bar.setRange(0, 0)\n\n    def handle_results(self, results):\n        current_tab_index = self.tabs.currentIndex()\n        output_text = \"\"\n        for item, result in results.items():\n            output_text += f\"{item}: {result}\\n\"\n\n        if current_tab_index == 0: # Calculate Tab\n            self.calc_result_text.setText(output_text.strip())\n        elif current_tab_index == 1: # Verify Tab\n             self.verify_result_text.setText(output_text.strip())\n\n        self.status_bar.showMessage(\"Operation completed.\")\n\n    def handle_error(self, error_message):\n        self.status_bar.showMessage(\"Operation failed.\")\n        QMessageBox.critical(self, \"Error\", error_message)\n        # Append error to the results text area for context\n        current_tab_index = self.tabs.currentIndex()\n        if current_tab_index == 0:\n            self.calc_result_text.append(f\"\\nERROR: {error_message}\")\n        elif current_tab_index == 1:\n            self.verify_result_text.append(f\"\\nERROR: {error_message}\")\n\n    def on_worker_finished(self):\n        if self.thread is None: return # Safety check\n\n        self.thread.quit()\n        self.thread.wait()\n\n        self.thread = None\n        self.worker = None\n        self.logic_instance = None # Clear reference\n\n        self.progress_bar.setValue(0)\n        self.progress_bar.setRange(0, 100) # Reset range\n        # Message is set by handle_results or handle_error\n        if \"Stopping\" not in self.status_bar.currentMessage(): # Avoid overwriting stop message\n             if \"failed\" not in self.status_bar.currentMessage(): # Keep failure message if error occurred\n                self.status_bar.showMessage(\"Ready\", 2000) # Show ready briefly\n\n        self.toggle_controls(enabled=True)\n\n    def toggle_controls(self, enabled):\n        \"\"\"Enable/disable controls during operation.\"\"\"\n        self.tabs.setEnabled(enabled)\n        self.calc_button.setEnabled(enabled)\n        self.verify_button.setEnabled(enabled)\n        # Enable stop button only when running\n        self.stop_button.setEnabled(not enabled)\n\n    def closeEvent(self, event):\n        # Ensure thread is stopped cleanly on exit\n        if self.thread is not None and self.thread.isRunning():\n            self.status_bar.showMessage(\"Stopping background task before closing...\")\n            if self.worker:\n                 self.worker.stop()\n            self.thread.quit()\n            self.thread.wait(3000) # Wait max 3 seconds\n        event.accept()\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    main_win = ChecksumApp()\n    main_win.show()\n    sys.exit(app.exec_())\n"
  },
  {
    "filename": "main.py